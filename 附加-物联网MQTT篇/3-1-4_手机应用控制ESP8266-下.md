# 3-1-4 手机应用控制 ESP8266（下）

> 太极创客 · 零基础入门学用物联网 · MQTT 篇
> 时长 19:06

## 本节要点

- 演示用手机 App 控制 ESP8266 开发板 LED 的完整效果，理解"手机即物联网客户端"的含义
- 在巴法云手机 App 中建立服务端连接档案：连接名称、客户端 id、服务端地址、端口号 8080 与 WebSocket 协议
- 明确手机 App 连接服务端必须使用 8080（WebSocket）端口，而不是 1883 端口
- 建立"LED 控制"开关组件：向主题发布 1 打开 LED、发布 0 关闭 LED
- 建立"LED 状态"文本组件与"LED 指示"灯组件：订阅状态主题，实时显示开发板 LED 的当前状态
- 学会修改组件设置与调整组件在界面中的排列位置

## 正文

您现在屏幕上所看到的这个 ESP8266 开发板，我已经把刚才给大家讲解的示例程序上传到开发板中了。另外您可以看到屏幕中有一个串口监视器，这个串口监视器将会显示出来开发板运行的过程中传输过来的信息。屏幕上还有一个手机录屏，这个手机录屏所显示的内容正是巴法云手机应用，我利用这个手机应用建立好了一个控制台，通过这个控制台我们可以控制 ESP8266 开发板。

当前我的手机还有这个开发板，请看我的手，可以控制到这个开发板，我们都在同一个房间里面。那么这个时候还体现不出来物联网的魅力。不过假如我现在这个开发板是在北京，而我的手机实时是在上海，那通过我这个手机应用，我就可以远程控制到我的开发板上面 LED 的点亮和熄灭。同时通过我的手机也可以获取到远在千里之外的这个开发板 LED 的状态，这就是物联网的魅力。

也正是通过巴法云这个手机应用，它会把我的手机变成一个物联网的客户端。通过这个客户端我们就可以订阅、发布 MQTT 的信息，那么通过这些信息，我们就可以获取到物联网设备的状态以及控制物联网设备。具体怎么操作，我接下来给大家演示一下。

### 一、手机应用控制开发板的效果演示

首先我们来看这个手机应用控制台上最上面的这个工具，这里是一个按键开关，通过这个开关我们可以控制开发板上的 LED 点亮和熄灭。请大家注意，我来打开开关，可以看到我的开发板上 LED 被点亮了；我来熄灭开关，打开、熄灭。

还是那句话，也可能我现在手机控制的是一个千里之外的设备，通过物联网就可以实时地控制它。另外请看，在我随时打开和关闭这个开关的时候，可能已经有朋友注意到了：手机应用上面这个 LED 的显示，它也是在随着这个开发板上的 LED 点亮、熄灭、点亮、熄灭，文本输出的信息也是最新的这个 LED 的状态。

好了，这个就是我们这个手机应用如何来控制 ESP8266 开发板这个实验演示了。那接下来，我来向你演示一下如何用手机应用，也就是用巴法云这个手机应用来建立我们这个控制台。

### 二、为手机应用设置服务端连接

现在您屏幕上所看到的这个界面就是巴法云手机应用的界面了。在刚一进入巴法云手机应用以后，我们需要做的第一件事情是要设置一个服务端的连接。我们要让我的手机能够实现和服务端连接的话，要先设置一个服务端连接的档案。那么这个设置是怎么来做的呢？

首先第一点，请看右下角这个加号，我们来点击这个加号。加好以后，这里就让我们输入连接服务端的一些相关的信息了。

首先我们要给这个连接的档案起个名字，随便起个名字，能让我们记住就好，这里我们就叫做"巴法云社区测试"。

接下来看后面这个客户端 id，这个地方很重要。为什么说呢？因为我们要想让我的手机作为一个客户端连接到服务端的话，那它的客户端 id 是不能乱填的，要怎么填呢？我们之前操作过。首先我要把我这个 iot_user，也就是我的这个用户名输入到这里，然后我们这台手机，我把它作为第三台设备。为什么第三台设备？第一台设备呢，我们用 MQTT.fx 这个软件已经用过了；第二台呢，是我刚才给大家演示的这个 ESP8266 开发板的那个程序里面，你也看到了，那个程序里面已经用到第二台设备了。那这里呢我就用第三台设备。

第三台设备的话，请看，接下来在用户名后面加一个下划线作为分隔，然后输一个数字三。这还没有输完，因为光输完第三台设备以后，还要继续告诉他这是第三台设备的 id，我再输个下划线，再输个 id，好了，这就是我的第三台设备的 id 了。

我们往下看，接下来我们要输这个 MQTT 服务端的地址，地址是什么呢？`bemfa.com`。地址说完了，我们接下来看左下角这个端口号，这个地方很重要，我们将要输入的这个端口号是 8080。看到好多朋友第一次见到我输这个端口是 8080，为什么呢？我们这个手机应用它和服务端进行通讯的时候，它不再使用 1883 这个端口了，而是要用 8080 这个端口，这个是 WebSocket 协议的端口。具体原因再说起来可能会比较复杂，在这里你就记住：如果是用手机来连接巴法云服务端的话，要用 8080 这个端口号。

那么右边这个网络协议这个地方，我们就保持默认这个 WebSocket 就可以了，不用去修改它。接下来请看，我们在高级设置里面，首先输入这个客户端它的用户名是什么，用户名是 iot_user，先输入用户名，然后再输一个下划线，然后再输入一个数字三，这和我的这个 id 是对应起来的，第三台设备，所以它这个用户名是三。然后输入密码，输密码的时候可能这个手机它的录屏是不允许录屏的，不过我可以告诉你，我这里输的是 password。为什么输 password 这英文拼写呢？因为这就是我的密码。

到这里所有信息都输入完毕了。我们来看一下：连接名称、客户端 id、服务端地址、端口号 8080、网络协议 WebSocket。然后这里有个地方要说两句，这个保持连接这个地方这里是 60 秒，没有问题，巴法云社区版它要求我们保持连接的时间是 10 以上的数字都可以，所以这里 60 是没有问题的。那么连接超时这个写 3000 就可以了，不用去管它。

全部都已经输入完毕以后，我们点击添加。当我点击添加完毕以后，我们可以看到上面这里显示出一个"巴法云社区测试"这么一个档案，我点击这个档案。点击以后请看右上角，这里是一个小云彩的标志，它上面画了一个勾，说明我们这个手机应用它已经连接到服务端了。如果您能看到这个云再加一个勾，那么就说明您的这个手机应用现在已经连接上这个服务端了。

如果您看到的是现在这个样子，云上画了一个斜杠，那说明您可能输入的这个连接信息可能有些不太正确的地方。那么在这种情况下，你要检查一下，怎么来检查呢？我们回到刚才这个连接的页面中，然后我们向左滑动这个档案，就可以看到这里有一个修改的按键，在这个页面中我们就可以修改刚才我们输入的信息了。由于我输入的信息都是正确的，所以我就不在这里重新修改了。

这样的话，我就已经成功地把这个连接的档案建立起来了。我们点击它，再次进入到这个连接中，可以看到右上角这个标志，说明我们已经成功地连接到服务端了。

### 三、认识控制组件列表

接下来我们就可以对我的手机应用添加控制组件了，这些控制组件将会让我们来实现对 ESP8266 开发板 LED 的控制。这些控制组件是怎么来添加的呢？请您看手机屏幕，在屏幕的右下角有一个加号，我来点击这个加号，我看到这一系列列表中的内容，正是巴法云这个手机应用给我们提供的一系列的控制组件。这些控制组件其实就是一系列的控制工具，有些是用来控制物联网设备的，有些是用来显示物联网设备信息的。

比如刚才我们在这个实验演示中您看到有开关这个控制组件，这个控制组件它是用来控制物联网设备的；同样的，我们还看到它有什么显示文本信息的，这个文本信息就是来显示其他物联网设备发过来的信息的。光这么说可能有点抽象，我们接下来给您做一个实际的演示。

### 四、建立第一个组件：LED 控制开关

接下来我就来添加一个开关控制组件。那开关控制组件在哪呢？在这个组件列表从上往下数第二个，这里有一个开关组件，我点击它，点击好以后就进入到这个开关组件的设置屏幕中了。

一上来第一个空间名称，这个地方就让我们来给这个开关起个名字，我们起个什么名字呢？由于这个开关它的作用就是来控制 LED 的，我们就给它起名字叫做"LED 控制"。

接下来我们来看主题这个地方，主题这个地方很关键。我先来把这个信息写进去：`iot_user`，我先写进去以后，然后您再来看一看您能不能明白我是在做什么：`iot_user/led_KZ`。我这里把这个主题信息写完了，写完以后,你知道我为什么这么写吗？

请您回忆一下，刚才我这个 ESP8266 开发板这个示例程序中，我们在设计程序的时候其实你见过这个主题，就在这里看我的鼠标高亮的地方。这个主题是在 ESP8266 开发板工作中它将会订阅到的主题，那么订阅好这个主题以后，它就会通过程序，每次这个主题收到新的消息以后，它就查一查这个主题收到的消息是不是数字一，如果是数字一它就会点亮 LED，否则它就会熄灭 LED。

那么回到我们这个手机应用中来，请看手机应用屏幕，对于我们这个开关控制组件来说，它是可以向某一个主题来发布信息的，具体向哪一个主题呢？正是刚才输入的这个地方，向这个主题来发布信息。具体发布什么信息内容呢？那接下来请看后面这两个空，一个是开关打开信息，一个是开关关闭信息，通过向这两个空输入内容，我们这个组件就向这个主题发布什么信息了。那么当我打开开关的时候，我在这里输入数字一；当我关闭开关的时候，我让它输入零。

也就是说我们打开开关，我的组件将会向刚才设置好的这个主题发布数字一，这个数字信息一旦发布出去以后，我的 ESP8266 开发板接收到以后，他看看是不是数字一，如果是的话它就会点亮 LED，从而实现这个 LED 的控制。反过来，如果关闭 LED 开关以后，那么它将会向这个主题发布数字零，自然而然我的 ESP8266 开发板一旦收到这个信息以后，它就会熄灭 LED。通过以上这个机制，我们可以利用这个组件来控制开发板上那个 LED 了。这个其实很直观，也挺简单的。

我们接着往下看，"发布前确认"，这个发布前确认事实上是可以让我们在每次打开或者关闭开关的时候，让手机应用问我们是不是真的要执行这个操作。而在这个演示中我们没有执行这个操作，所以这里我就留空，如果您感兴趣的话可以试一试，这里面添加一些信息。后面请看这里，"消息质量"还有这个"保留"这个地方，您可以根据您自己的喜好来进行设置。那消息质量也就是 QoS 了，它有 0 级、1 级、2 级，你可以自己来定，我这里就不改了；包括这个保留这个地方，我就不再去改变它的这个设置了。

另外请看我在这后面有一些组件样式的设置，它是可以设置我们这个开关它这个颜色、大小等等信息，这里我们就使用这些默认信息不去改变了。输入完信息以后，接下来我们就可以看最后这个"新增保存"这个按钮，我点这个新增保存按钮，点击好了。点击完新增保存以后，我们就建立好了一个控制组件了。

这个控制组件，请看我一旦点击它，它将会打开，它事实上是向刚刚设置好的这个 `iot_user/led_KZ` 这个主题发布了一个数字一的信息，那么这个数字一就可以控制 ESP8266 开发板了。同理，我一旦关闭它，它就发布数字零，这样就可以实现对这个 ESP8266 开发板上 LED 的控制了。

### 五、建立第二个组件：LED 状态文本记录

建立好这个开关控制组件以后，接下来我们就来建立第二个组件了。第二个组件是用来显示 LED 开关状态的，是哪一个组件呢？我们还点击右下角这个加号，我们是通过请看从下往上数，第三个地方有一个文本记录这么一个组件，我们来点击这个组件。

这个组件相对来说设置的比较简单，它的作用就是将要订阅某一个主题，并且把订阅到这个主题的信息显示在手机屏幕中。那么我来设置一下这个组件的内容。首先在空间名称中，我给它起个名字就叫"LED 状态"。然后我要输入这个主题了，这个主题正是我们这个组件它将要订阅的某一个主题，具体主题内容是什么呢？我这里输入的这个主题名字，请大家看屏幕：`iot_user`，然后斜线，然后 `led`，再加上一个下划线 `ZT`。

说完这个主题以后，我相信你应该明白我的目的是什么了，这个很直观。因为我的这个 ESP8266 它将会通过程序把 LED 的状态发布到这个主题中，所以我在这个手机应用中我就订阅这个主题，这样的话就可以收到 ESP8266 开发板它所发布的信息了。同样的，后面这个消息质量等等这些其他的设置，我就保持默认就可以了。我点击新增保存。

那么点击完新增保存以后，我们看到这个组件已经出来了。我是打开这个 LED，请看我打开 LED 以后，我们就马上收到这个 LED 状态了。虽然你屏幕上没有看到这个 ESP8266 开发板 LED 是不是点亮和熄灭，但是通过这个 LED 状态我们就知道开发板上的 LED 是打开了。我再点击，好，关闭了。好了，这个就是文本组件的设置方法。

### 六、建立第三个组件：LED 指示灯

那最后我们再来建立第三个组件，也就是这个专门显示 LED 状态的一个 LED 的指示灯式的组件。我同样还是点击右下角的加号，加号以后我们来找这个 LED 组件在哪，从上往下数，第六个这个组件，我们点击它。

这里同样的还是先要给它起个名字，我们就给它起名字叫做"LED 指示"。那么主题呢，同样的这个组件它也会订阅某一个主题，订阅哪个主题呢？它就会订阅 ESP8266 开发板当前 LED 状态的这个主题，也就是请看屏幕上，在这个程序中 ESP8266 开发板它将会向请看我鼠标高亮的这个地方，向这个主题发布当前 LED 还是点亮和熄灭的一个状态。那么我在这里就把这个主题输入到我们这个手机应用中来：`iot_user` 然后是斜线 `ledZT`。

好了，这个主题输入完以后，接下来就要输入这个 LED 点亮信息和 LED 熄灭信息了。那么我们在 LED 点亮信息中，我们将输入一个全部大写，注意是全部大写：`LED ON`；然后在这个 LED 熄灭信息中，我们输入全部都大写：`LED OFF`。

为什么输入这些信息呢？请您回忆一下，在我们这个程序中，每当这个 LED 点亮的时候，它将会向那个汇报当前 LED 状态的主题中发布 `LED ON`，这里全部都是大写，中间一个空格，输入这么一个信息；那同样的熄灭的时候就会输入这么一个信息。我们就要把这两个信息照搬到我的手机应用中来。

我们这个 LED 它这个点亮和熄灭的控制机制呢，它就会看看它所订阅到的，请看我鼠标，你看手机屏幕上这个鼠标高亮这个它所定义的主题，当这个主题收到的信息是不是和我的手机这里设置的这个信息是一模一样的，如果收到一模一样的信息的话，它就会点亮 LED；如果是收到请看这里这个鼠标高亮列表，那它就会熄灭 LED。

同时后面这里可以设置一下点亮的时候这个灯的颜色，就使用默认红色就行了，不用管它，熄灭的时候颜色也可以设置。好，其他的什么消息质量这些设置我就保持默认，不用去管它了。然后点击新增保存。那么我们可以看到这个组件已经显示出来了，我来试试看看它好不好使。点击，没有问题，当这个 LED 点亮的时候，这个指示灯也点亮了，没问题；这个指示灯熄灭了，也没问题。那么这几个组件已经建立好了。

### 七、修改组件设置与调整排列位置

假如您在这个设置过程中出现了一些问题，想去修改一下这个设置怎么办呢？请看，我们现在就要修改一下这个 LED 这个指示的组件，修改一下它这个设置。在这个组件的右上角这里有三个点，我点击这三个点，点击三个点底下就显示了一个新的屏幕，这个新的页面中他就会告诉我们可以编辑或者删除，或者向上移动或者向下移动这个组件。我点击编辑这个按键，那么一点击编辑，我们就回到这个 LED 设置的页面中来了，这里我们就可以对 LED 进行一些修改。我们改一改它，我们给它改个颜色，我给它改成一个绿灯，以及设置，修改好以后我们点击编辑保存。

这样的话我们来看看这个灯是不是颜色变了，我们打开 LED，好，没问题；关闭 LED，没问题。那同样的，我们也可以改变这几个组件在空间中的位置，比如说把这个 LED 往上挪一挪，然后我们可以看到 LED 到我的上面来，我们把这个开关向下挪一挪，没问题。

这个就是如何建立一些基本的巴法云组件的演示。在后续的课程中，我们还会给您对其他这些组件做更深一步的讲解。好了，那么关于这个如何使用手机应用来控制 ESP8266 开发板上的 LED 课程内容，我们到这里结束了，非常非常感谢您，我们下节课再见。

---

*本文稿由 AI 从 B站 AI 字幕整理，如技术描述与原视频有出入，请以原视频为准。*
