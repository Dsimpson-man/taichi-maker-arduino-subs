# 1-4 服务端连接操作

> 太极创客 · 零基础入门学用物联网 · MQTT 篇
> 时长 32:12

## 本节要点

- 安装并使用 MQTT.fx 客户端软件，把电脑变成一个 MQTT 客户端
- 在 MQTT.fx 中新建连接设置：填服务端地址、端口号 1883、Client ID、Keep Alive、Clean Session
- 连接、订阅主题、发布信息、断开连接，并用 MQTT.fx 完成一次完整的发布/订阅测试
- 安装 PubSubClient 库，编写 ESP8266 连接 MQTT 服务端的示例程序
- 理解程序中的 WiFi 连接、setServer、connect、connected、loop 等函数的作用
- 掌握使用 ESP8266 的 MAC 地址构成唯一 Client ID 的方法，并理解心跳机制在 loop 中的调用要求

## 正文

大家好。在上节课里，我们学习了连接 MQTT 服务端的理论知识，那么这节课我们将实际来操作，使用我们的电脑以及 ESP8266 来实际地连接 MQTT 服务端。我们点击 1-4，也就是我们这节课的讲义内容。

首先我们先来学习电脑客户端连接 MQTT 服务端的操作。要想把我们电脑变成一个 MQTT 客户端的话，我们首先要给这个电脑安装一款 MQTT 客户端软件。MQTT 客户端软件有很多，我们经过使用发现，这款叫做 MQTT.fx 的免费 MQTT 客户端软件在功能性以及易用性方面都非常出色，所以在接下来我们将使用这款软件。

那么使用这款软件，首先我们就要把这款软件安装到我们电脑中来了。安装这款软件有两个方法。第一个方法，你可以到 MQTT.fx 的官网去，网址在这里，这个网站由于一些原因有些朋友是无法打开的。所以如果您发现您是无法打开这个网址、无法下载这个软件的话，那么您也可以通过点击这里进入我们太极创客网站的这个下载页面。通过这里你可以看到这个 MQTT 客户端软件有一系列的下载链接，那么这系列下载链接针对不同的操作系统版本，如 Windows 64 位的版本、Windows 32 位版本，还有苹果电脑这个操作系统。请您注意选择与您自己电脑操作系统相符的版本，不要下载错了版本，否则安装之后是没法正常运行的。如果您想直接进入这个下载的页面的话，您可以通过首页，首先这里有一个"下载"这么一个链接，点击它，将进入到我们这个网站的下载页面。在这个上面，你看这个列表中可以看到这个 MQTT 客户端软件下载链接，点击它就可以找到 MQTT.fx 的下载链接了。

在这里提醒一下，我们建议您尽量使用 MQTT.fx 官网下载这个软件，原因是官网上的软件毕竟是最新版本的，而我们这里只是为了提供给那些无法打开官网的朋友，给他们提供这么一个下载链接的列表，所以我们这里的软件可能它的更新速度不如官网更新速度更快。好，那接下来就请您根据刚才我给您指示的这几种方法把这个软件下载并且安装到您的电脑中去，这个安装过程非常简单，我在这里就不再演示了。

当你把这个 MQTT.fx 软件安装好以后，接下来就可以打开它了。那么打开了以后，您可以看到屏幕上您所看到这个界面，和我这个屏幕中的界面是一样的。好，那接下来我们就来建立一个新的 MQTT 服务端的连接。如何来建立呢？请看在这个界面的上方，这里有一个像齿轮一样的这么一个图标，我们点击它。这个新弹出的界面是用来设置 MQTT 服务端连接的，我们可以通过这个界面来添加一个新的 MQTT 服务端连接。

也许您所看到的这个界面和我这里有点区别，为什么呢？您左侧这里可能没有这么多信息内容，原因是我这个软件里已经用了一段时间了，所以我已经建立了一系列的 MQTT 服务端连接了。那么每一个这里这个列表中，请看鼠标这里列表中的每行字都是一个 MQTT 服务端连接的设置。好，那么我们接下来将要建立一个新的 MQTT 服务端连接，如何建立呢？请看左下角这个加号，这个减号，加号就是增加一个新的连接，减号就是把之前的一个连接给它删掉。比如说我现在选中了这里这个蓝色选中它以后，如果我不想要这个连接的话，点这个减号它就被删掉了。但是我不想删掉它，我这里给你演示的时候如果要添加一个连接，那我这里点这个加号。这个加号和减号的用法您要分清楚，加号对应的是新建，减号对应的是删除，而且删除是针对当前被选中的那一条连接生效的，所以删除之前一定要先确认自己选中了哪一条。

请注意，随时点击加号以后，我们左边这个列表上面出现了一个新的这一行字，写着 new profile，new 是新的意思，profile 是设置的意思。换句话说，我们通过点击这个加号，我们建立了一个新的连接设置，这个连接设置它有一个名字，名字默认的叫做 new profile，所以在这里我们看到的就是 new profile。我们可以改一个名字，改成中文名字吧，改成叫做"太极创客 MQTT 教程示例"。现在我们把这个 new profile，就是新的这个连接这个设置名称，把它改名叫做"太极创客 MQTT 教程示例"了。好，这个名字写好以后，我们继续来看。

下面这个地方，这个是告诉 MQTT.fx 这个软件，我们要告诉它我们现在连接的这个服务端是什么类型的服务端。好，这里有两个选项，一个是 MQTT broker，一个是 Google Cloud IoT，我们选上面这个，也就是默认的这个。这个就是 MQTT 服务端的意思，broker 就是服务端的意思，所以我们这里要连接 MQTT 服务端就选它就可以了。

好，那我们接着往下看。请看这里这个写着 broker address 的地方，这是让我们输入服务端地址的地方。如果你有个人自己的 MQTT 服务端的话，你可以把个人的 MQTT 服务端地址输在这里；如果没有的话，我们是可以使用网上的免费公用 MQTT 服务端的。这些 MQTT 服务端它们的作用就是让我们来学习 MQTT 协议使用的。那么这个 MQTT 公用服务端在哪里可以找到呢？我们可以看到，请看我们还回到这个 1-4 MQTT 服务端连接操作，就刚才给大家讲解这个讲义的页面。这个页面的底部，请看这个页面底部有一个"连接公用 MQTT 服务器列表"，点击这个链接就可以打开这个公用 MQTT 服务器列表的界面了。我们已经整理了一系列的公用 MQTT 服务端的地址信息以及它们端口信息等。那么这些 MQTT 服务器，它们有的是在国内，有的是在国外。

我在这里建议我们还是优先使用国内的服务端，因为这个国内服务端对我们来说连接起来会更稳定一些，国外服务端可能有一些其他的原因，有时候会连不上。那么这个国内的服务端，这里有叫"燃也物联"的这么一个服务端，好，它的官网在这里，它的服务端地址是 test 点 ran ye（汉语拼音"燃也"），横杠 iot 点 net。那么我们把地址输入到 MQTT.fx 这软件中这个服务端地址的地方。好，test 是测试的意思，然后"燃也"汉语拼音 ran ye，横杠 iot 点 net。这个公用服务器列表里除了地址之外，还会给您提供一些其他的说明信息，比如服务端是否支持订阅、是否支持保留消息等等，您在实际选用的时候可以对照着看一看。当然如果您有条件搭建属于自己的 MQTT 服务端，例如使用 mosquitto 或者 EMQX，那也是可以的，那样在设置上会有更大的自由度。对于刚刚开始学习的朋友来说，先用公用服务端把整个流程跑通，是最省事的做法。

好，这里说完以后，接下来这里这个地方是让我们来输入这个连接服务端的端口号。好，这个端口号我们同样也可以在这里找到，你看这里写着 TCP 端口是 1883，TLS 端口是 8883。端口选哪个端口呢？这个 1883 端口就是纯的 TCP 的端口，通常我们在刚刚开始学习使用 MQTT 协议的时候用这个端口就可以了，因为这个端口的好处就是比较简单直接，就是通过 TCP 来进行连接。但有些我们将来在实际操作中，如果想要对这个传输进行加密的时候，我们就需要用到这个 TLS 端口了，这个端口通常是 8883 端口。好，那现在我们刚刚开始接触 MQTT 协议，我们就用这个 1883 这个端口就可以了。好，我们还回到这个 MQTT.fx 中，我们把这个 1883 端口把它输在这里。

好，那我们继续往下看。我们看后面这里，这个地方是让我们输入 Client ID 的地方。诶，Client ID 我不知道您还有没有印象，我们上节课在讲解连接 MQTT 服务端这个理论知识的时候给大家讲过，MQTT 客户端要向服务端连接的时候，它要发送一个连接请求，这个连接请求是一个 CONNECT 数据包，这个数据包中有一系列的数据，其中有一个数据就是 Client ID。好，那么我们在这个 MQTT.fx 软件中这里所输入的这个信息，正是用于我们这个连接请求 CONNECT 数据包中这个 Client ID 这个数据信息。那接下来我们就可以把 Client ID，我们想要给这个客户端起一个名字，叫什么名字输在这里就可以了。您可以看到，在 MQTT.fx 中填写的这一项，和我们上节课讲的 CONNECT 数据包里的 Client ID 是完全对应的，这也是我们为什么要在上节课先把理论知识讲清楚的原因，只有知道每个参数是干什么用的，实际操作的时候才不会填错。

我建议你在这里输入英文字符，并且不要输入特殊字符。如果图省事的话，那直接在这里点这个 generate，这时系统将会自动给我们生成出一系列的这个字符，那这个就是我们自动生成的一个客户端 ID。如果您想给它起个名字的话，你可以在这写下去就可以了。有一点是很重要的，就是尽量起这个名字不要跟别人重，不要简单地说我这个 Client ID 就写一个叫什么 "id" 这样的，这种太简单的 Client ID 经常会跟别人重掉，毕竟我们用的是一个公用的 MQTT 服务端。如果我们这个 Client ID 和别人重名的话，那这个是不允许的，所以呢我建议你点这个自动生成一个随机的 ID 就可以了。好，这个就是 Client ID。

接下来我们继续往下看。我们下面再看这里，这里有一系列的信息，其中请看这里 Keep Alive interval，诶，Keep Alive 又看见它了。我们是什么时候见过它的呢？在 CONNECT 数据包里面这个 Keep Alive，它是什么还记得吗？心跳时间间隔。好，那么同样的这里这个我们输入想要的时间输入在这里就行了，如果图省事的话，那就默认 60 秒就可以了。这里的心跳时间间隔和上一节课讲的 Keep Alive 是同一个概念，只不过在这里它是在图形界面上填写，而在编写程序的时候它是通过代码来设置的，所以您会发现，理论知识里学过的每一个参数，在实际操作中都能找到它对应的位置。

好，那我们继续往下看。请看在这个心跳时间间隔下面这里写着 Clean Session，这个 Clean Session 我相信大家听到这个名字以后是不是也很熟悉呢？我们上课刚刚给大家讲过 Clean Session 它的作用，我们把它设置为 true 或者 false，对不同的重要性的客户端来说，我们可以对它进行相应的设置。那这里当我们 Clean Session 打上勾的时候，就说明我们要连接的时候这个数据包中 Clean Session 这个地方设置为 true，打上勾设置为 true，不打勾设置为 false。好，我们在这里就默认的打勾就可以了，以 Clean Session 这个形式让客户端来连接服务端。因为我们在做演示，这个客户端并不是什么重要的客户端，所以这里保持默认的打勾状态就是合适的。

好，那到这里我们这个设置就初步完成了，其他内容设置为默认的这些信息就可以了。另外我们在后期课程中还会给大家讲解更多的相应的设置内容。好，到目前为止，你输入这些相应的信息就可以了，请现在再检查一下这一项一项的内容是不是和我设置的是一样的。如果检查完毕、设置没有问题的话，接下来我们点击，请看右下角这里点击这个 OK。

好，那么点击完 OK 以后，你可以看到这里显示出来是我们刚刚设置好的连接的设置。确认一下的话，那我们可以看到这个点这个齿轮这个图标，在这个齿轮图标点击完以后，同样弹出这个连接的设置的内容，其中在左下角这里多了一个这"太极创客 MQTT 教程示例"的这么一个连接，这个是我们刚刚设置好的，这些信息都是我们刚刚设置好的，假如你想将来修改的话，可以在这里修改一下就可以了。以后您再打开这款软件，这个连接设置还在，直接选中它点 connect 就可以再次连接，不用每次重新填写一遍。

好，那么接下来就到了非常非常关键的一步了。什么关键一步呢？我们将要用我们刚刚设置好的这些信息来连接 MQTT 服务端了。怎么来连接呢？请看这里有个 connect 这么一个按钮，点击 connect，好，假如你看到右上角这里显示一个绿色的这么一个指示灯的话，那说明服务端已经连接成功了。如果这里是红色的话，那说明就是连接失败。另外你可以看到，刚才我们这个 connect 按钮我们点击完以后，连接成功以后这个按钮变成灰色的了，不能再点了，而反过来这个 disconnect 这个按钮倒是变成了一个可以点击的状态了。是什么原因呢？是因为 connect 已经成功了，那么现在我们要想断开跟服务端连接，点击这个按钮 disconnect 就可以了。

如果您点击 connect 之后发现指示灯是红色的，那就说明连接失败，这个时候您需要检查一下几个地方：一是服务端的地址有没有输错，二是端口号是不是 1883，三是 Client ID 是不是和别人重名了，四是您当前的网络能不能正常访问这个服务端。把这几项挨个排查一遍，一般就能找到连接失败的原因了。这一点和后面我们讲 ESP8266 连接服务端时通过返回码查找原因是同一个思路。

不过我们先不断开这个连接，我们先再测试一下。我们现在先给大家做一个演示，这个演示就是让我们这个客户端来订阅一个主题，并且用这个客户端向该主题发布一个信息，看看能不能收到。好，那接下来我们就要用这个客户端来订阅一个主题了，怎么来订阅呢？请看这里，这里写着 subscribe，subscribe 英文订阅的意思，我们点击它，然后在这个对话框中我们输入我们将要订阅的主题名称。好，随便起个名字，我们就叫 TC-make 好，就订阅一个主题叫 TC-make 就好了。输入完这个主题名称以后，我们点击右侧这里只有一个按钮，我们点击它。好，所以我们点击，我们可以看到底下这里多出了一行，这个地方就是告诉我们现在已经成功地订阅了一个主题叫做 TC-make。另外这里写一个 0，说明了这个订阅这个主题还没有收到任何的信息。为什么没有收到任何信息呢？因为我们还没有向这个主题发布任何信息。

那接下来我们就来向这个主题来发布信息，怎么来发布呢？请看这里有一个 publish，publish 发布信息的意思。我们向 TC-make，注意我这里输入的这个就是发布主题的名称，我们要向这个主题发布一个信息，发布什么信息呢？我们就发布一个测试信息 test-123。好，发布的这个主题信息内容是 test-123，输入完这些信息内容以后，接下来我们点击这个 publish。点击完以后我们怎么知道这个信息被收到了呢？我们看看我们订阅的这里，喂，请看刚才这里是数字 0，现在变成 1 了。另外请看在这里显出来 test-123。为什么呢？因为我们刚刚通过这里向 TC-make 发布了一条信息 test-123。我们再发一个，Test 789，publish，诶这变成 2 了，收到两条信息了，test-789。好，第一条信息 test-123，第二条信息 test-789。通过这个简单测试，我们明确地知道了现在我们客户端已经跟服务端确定地连接上了，实际上我们已经可以跟这个服务端发布并且订阅信息了。

好，当完成这个操作以后，接下来我们就可以断开跟服务端的连接了。怎么断开呢？点击这个 disconnect 这个按钮点击它，所以我们点击，我们看到这里这个绿色这个指示灯变成灰色的了，我们已经跟服务端断开连接了。好，到这里关于如何使用 MQTT.fx 这款客户端软件来连接 MQTT 服务端的这个操作演示，我们就给你讲解完毕了。在后续课程中，我们还将要使用这款软件来进行更加深入、更加高级的这个 MQTT 的操作。好，到目前为止学会如何连接、如何简单地发布订阅一些信息就足够了。您可以把刚才这套流程自己再走一遍，特别是订阅和发布这两个动作，因为它们是 MQTT 协议中最核心的两个操作，在后面使用 ESP8266 开发板的时候，我们也同样要完成这两件事，只不过那时候是通过编写程序来完成，而现在是点点鼠标就能看到效果，所以先用 MQTT.fx 把概念建立起来，对后面的学习非常有帮助。

好，了解了如何使用电脑来连接 MQTT 服务端的操作以后，接下来我们来学习如何使用 ESP8266 开发板来实现对 MQTT 服务端的连接。要想使用 ESP8266 开发板连接 MQTT 服务端的话，首先我们要先安装一个库，这个库的名字叫做 PubSubClient 库。这个库您可以通过以下几个渠道来获取到。首先您可以通过这个库的官网地址或者 GitHub 这些网址来获取到，这个网址你可以就在我们这个 1-4 这个讲义中就会找到。好，在这里这两个网址，点击它们就可以进入官方下载这个库的页面了。

如果您通过这两个链接下载有困难的话，您可以通过我们的网站来下载这个库，具体如何实现呢？首先您可以进入到我们的网站首页，好，在我们的网站首页中上面就有一个下载链接，好，那下载链接这里有一系列的列表，这个列表中有一个叫"Arduino ESP8266 第三方库下载"的这么一个链接，我们点击它，在这里我们可以找到 PubSubClient 库的这个下载的渠道，这里有一个百度网盘的链接，这里有个 GitHub 的连接。另外在我们这个 1-4 这个讲义的页面中，我也把这个链接提供给您了。安装第三方库的方法，和我们在基础知识篇中安装其他库的方法是一样的，您可以按照那里讲过的方式，把下载好的库放进 Arduino 的库目录中，重启 Arduino 之后就可以在程序中引用了。

好，当你把这个 PubSubClient 库把它下载并安装到 Arduino 中以后，接下来就可以把请看这一页面中的这个示例程序把它复制并粘贴到 Arduino 中。我们看这段示例程序它的具体内容。好了，现在程序已经连接到 Arduino 中了，接下来我们来看看主要内容。首先我们看刚一开始，程序有两个井号 include，这两个井号 include，第一个很眼熟，我们以前见过很多次了，不在这浪费时间了。在这里这个是这是一个新的一个库，这是什么库呢？这就是我们刚刚安装好这个 PubSubClient 库。好，我们这个程序两个库把这个地方是井号 include 做好以后，接下来我们看后面这一系列的常量字符指针，其中这个 ssid 是我们将要用我们 ESP8266 连接 WiFi 的这个名称，后面这个 password 是连接 WiFi 的这个密码，您要根据您实际的这个 WiFi 名称和 WiFi 密码来填，到这里这样的 ESP8266 才能连上 WiFi。这里要特别提醒一句，ssid 和 password 这两项一定要和您自己路由器的实际信息一致，大小写也不能填错，否则开发板是连不上 WiFi 的，这一点和基础知识篇中连接 WiFi 的操作是完全一样的。另外，由于后面还要连接 MQTT 服务端，所以您的 WiFi 必须是可以正常上网的网络，如果只是局域网内的路由器而没有连接外网，那么开发板同样连不上公用的 MQTT 服务端。

后面这个这是我们第一次见到它，其实很好理解，在这里请看这两个引号中间所输入的内容，它正是我们即将连接的这个 MQTT 服务端的地址，你看 test 点 ran ye 杠 iot 点 net，这是我们要连接的 MQTT 服务端地址。好，那么我们给它起个名字就叫 mqtt_server，MQTT 服务器的意思，后面我们会在程序中用到这个信息。

好，那我们继续往下看。请看这里这里首先建立了一个 WiFiClient 对象，对象名称就叫 WiFiClient，这个是用来连接 WiFi 用的。后面我们请看这里，我们建立一个 PubSubClient 对象，这个对象名称叫做 mqttClient。注意，这个对象我们后面在程序中会反复用到它，所以请大家留意。好，我们介绍这个 PubSubClient 对象以后，我们要给它提供一个参数，这个参数是什么呢？正是刚刚建立好的这个 WiFiClient 对象，把这个对象的名称把它放到这个括号中作为参数提供给 PubSubClient 这个对象。好，那么建立好了这个 PubSubClient 对象以后呢，紧跟着进入到 setup 函数。setup 函数首先启动串口通讯，然后设置为 station 模式，也就是无线终端模式。好，接下来执行这个 connect WiFi 这个函数，这个函数的作用就是让我们 ESP8266 能连接上 WiFi，这个函数在程序的后面。好，在这里这个函数中的内容我们之前的课程中给大家反复地讲过很多次了，我就不在这里再浪费大家的时间了。如果您不知道怎么实现的话，说明你有可能上一个基础知识篇里的内容你已经忘记了，或者你还没有看过那里的内容，那我建议您看一看那里的内容，就会知道如何来实现 ESP8266 连接 WiFi 这个操作了。好，那么这个函数我们就到这里。

我们继续往下看。请看接下来的这里，mqttClient，这是什么东西呢？这正是我们这个 PubSubClient 的 mqttClient 这个对象。我们用这个对象调用了一个 PubSubClient 库中的一个库函数，这个库函数名称叫做 setServer，set 是设置的意思，server 是服务器的意思，那么说明这个函数的作用就是用来设置服务器的。那如何来设置这个 MQTT 服务器呢？请看这两个参数。第一个参数是 MQTT 服务器的地址，请看这里这个 MQTT 这个地址我们在这里输入进来的 mqtt_server，这是第一个参数。后面的参数 1883，这是 MQTT 服务器连接的端口号，这个信息我们之前给大家介绍过，咱们这网站这个 1-4 讲义界面中底部有一个公用 MQTT 服务器列表，好，这里"燃也物联"连接服务器，这上面的就是服务器的地址，下面这个是 TCP 端口号 1883。那么这些信息都是用的是"燃也物联"这个服务器的一些信息。

虽然我们这个程序的作用就是让我们的 ESP8266 连接"燃也物联"的这个 MQTT 服务端，假如你想要 ESP8266 来连接其他的公用服务端的话，好，比如说连接这个 mosquitto 这个服务端的话，那么在这个 MQTT 这个服务器地址这个地方要输入的就是 test.mosquitto.org，把这个信息把它放到程序刚开始这个 mqtt_server 这两个引号中输入这里这个高亮的地方这个内容，端口同样都是 1883，这不用变。好，我们还是使用"燃也物联"，因为它是国内的服务器，所以我们还是使用"燃也物联"的服务器。

好，刚才我说到这里，这个是设置 MQTT 服务器的一个一系列的操作。接下来请看这里，这个是调用了一个函数，函数的名称叫 connect，连接 mqtt_server，连接 MQTT 服务器的这么一个函数，这个函数在哪呢？在程序的后面，就在这里。好，下面我们一起来看看这个连接 MQTT 服务器的这个函数的内容。这个函数的内容非常重要，请您务必要开始注意。

首先我们在这个函数中建立了一个字符串型的变量，这个变量名称叫做 clientId。看到这个 ID 这个名字，我估计您就猜到这个字符串变量的作用了。这个知识内容我已经给大家讲过很多次了，当我们的客户端在连接 MQTT 服务端的时候，首先它要向 MQTT 服务端发送一个连接请求，这个 CONNECT 数据包就是它的连接请求，这个 CONNECT 数据包中有一个叫做 Client ID 的这么一个数据，那这个数据它的作用就是告诉服务端我叫什么名字。那么在这里这个字符串里所存储的内容正是告诉服务端它的名字是什么，也就是 Client ID 是什么。

我们来看这个 clientId 这个字符串里存储的内容，它由两部分组成。前半部分请看这里是 ESP8266 横杠，后面这个加号，每回在这个 ESP8266 横杠后面我们增加了一部分内容，这部分内容是什么呢？可能有朋友看有点陌生，实际上我们这里是调用了一个库函数，这个库函数将会把 ESP8266 开发板的这个 MAC 地址把它返回。调用的这个地方，可能有的朋友对这个 MAC 地址这个概念不是很清楚，我在这给大家简单介绍一下。MAC 地址就是我们每台联网设备它有一个独立的序列号，所谓独立序列号就是这个设备它这个序列号是它所特有的，它是不会跟其他设备有同样的序列号的。

我们把这个设备的 MAC 地址作为我们 ESP8266 的 Client ID 中的一部分，它的目的，我之前课程中给大家讲过，在同一个服务端上所连接的客户端它这个 Client ID 是不能够重名的。那么这个就是为什么我们在这个程序中我们利用 ESP8266 它特有的这个序列号这个 MAC 地址来作为它 Client ID 的一部分，也就是我们每个人手中的 ESP8266 开发板，当它连接同一个服务端的时候，它所使用的 Client ID 是不会重复的。绕了这么一大个弯子，说白了就是让我们每个人连接的时候不要使用相同的一个 Client ID，这一点很重要。

好，我们继续往下看。接下来请看后面这里，这里使用了一个逻辑判断语句，这个逻辑判断语句基本上就是这个函数后续的内容，就是这么一个逻辑判断语句。好，那么这个逻辑判断语句的判断条件，请看这里，是利用了 PubSubClient 的这个对象调用了叫做一个 connect 这个函数，这 connect 函数是 PubSubClient 库中的一个库函数，它将会让我们这个 ESP8266 实现对 MQTT 服务端的连接。那么在实现服务端连接的时候，它要提供一个参数，好，这个参数什么东西呢？这个参数的内容正是这个 clientId，也就是我们刚刚建立好的 Client ID，告诉服务端它的 Client ID 是什么。那么这个 connect 函数将会有一个返回值，如果我们这个 ESP8266 成功连接上了服务端的话，那么将返回真；如果连接失败了的话，那么这个 connect 函数将会返回假。

好，我们看一下，如果成功连接了，那么将执行后面这一系列语句，这一系列就是通过串口监视器输出一系列信息，告诉我们现在 MQTT 服务器已经连接成功了，以及 MQTT 服务器的这个地址是什么，以及我们当前使用这个 ID 是什么，通过串口监视器的输出一些信息，告诉我们连接的情况。所以程序一旦运行起来，我们就可以通过串口监视器这一个小窗口，把客户端连接服务端的整个情况看得清清楚楚，这也是我们在调试 MQTT 程序时最常用、最有效的手段。如果这个连接的时候 connect 失败了、返回假的话，也就是 else 后面这个执行的内容，这部分的内容请看，首先通过串口监视器告诉我们连接失败了。然后请看这里后面这个地方，诶，这个输出的内容是调用了一个 PubSubClient 库中的一个库函数，这个叫 state 的库函数，这 state 库函数是什么东西呢？

请大家回忆一下，我在之前的课程中给大家讲过，当我们这个客户端向服务端发送连接请求以后，然后服务端接到请求以后它会返回一个数据包叫 CONNACK，这个数据包里面 CONNACK 数据包中的信息内容，请看 CONNACK 数据包在这里，它有两个，一个 Session Present，一个 Return Code，这 Return Code 这个内容正是我们这个程序中这个 state 函数中它所返回的内容。也就是说通过这个 state 参数，我们将可以得到什么呢？得到这个连接返回码啊，这连接返回码你还有印象吗？它是服务端告诉我们当前这个客户端连接的状态。那么这些返回码的具体内容，您可以通过我们这个网页中可以看到。说白了，我们这个程序中就是如果一旦这个连接失败了的话，我们让这个 ESP8266 通过串口监视器输出一下，输出什么内容呢？输出服务端给我们一个返回码，我们可以看看到底是什么原因产生的失败，我们通过这些数字比如说 2、3、4 可以知道到底是什么原因导致了我们这个连接失败。好，那么这一段就是这个逻辑判断，以及到这里就结束了，我们这个 connect mqtt_server 这个函数的内容也就结束了。

好，那我们还回到 setup 函数中调用这个 connect mqtt_server 这个函数这个地方。刚刚我们到这里，到这里这个 setup 函数就结束了。我们接着来看 loop 函数中的内容。loop 函数是一个 if else 逻辑判断，这个逻辑判断的条件，请看也是调用了一个 PubSubClient 库中的一个库函数，不过这个库函数请看它是 connect 加 ed，这是 connected，这是什么呢？这是判断当前我们这个 MQTT 客户端也就是 ESP8266，它有没有成功地连接到 MQTT 服务端上。好，我再说一遍，这个函数它是可以检查一下我们的 ESP8266 有没有连接到 MQTT 服务端上，好，如果连接成功了的话，那么这个地方它就会返回真，如果连接失败的话它就返回假。注意，这是检查，它不会执行连接的这个动作，它跟刚才我们在这个函数中所讲解的这个 connect 函数是有区别的，这是 connect，这是 connected，这是检查状态，这是连接服务端。这两个函数的名称只差几个字母，但作用完全不同，请一定要注意区分，不要用错。

换句话说，我们 ESP8266 它在运行过程中将会不断地进行一个检查，检查什么呢？检查一下我们当前 ESP8266 有没有成功地连接到 MQTT 服务端上。如果连接成功的话，它将会执行这里，请看这里的是什么，好，这里请看又是调用了 PubSubClient 库的库函数，这函数名字叫 loop，诶 loop 跟这里这个函数名字是一样的，不过这个 loop 它它的作用是来让我们的客户端来执行这个心跳机制的。还记得心跳机制吧，让我们这个客户端定时地向这个服务端发送一个心跳信息，告诉它我还跟你连着。请大家注意，为了确保客户端的心跳机制能够正常运行，我们要把 PubSubClient 库中的这个库函数我们一定要把它放在 loop 函数中，确保它能够经常性地被调用。如果我们在这个 loop 中可能出现了比较长的这种 delay 这种，可能会导致我们这个 loop 没有得到及时的调用，这种情况下会导致我们的客户端的工作不稳定，所以我建议您如果使用 PubSubClient 库的话，那么尽量不要在这 loop 中使用 delay 函数。这个就是 ESP8266 的客户端它在与服务端保持连接的这个状态下操作执行的程序内容，让它执行心跳机制。

好，如果请看这 connect 函数返回假说明什么呢？说明我们这个 ESP8266 的客户端它和服务端连接断开了，这个时候它就会请看执行这里，这是什么东西呢？connect mqtt_server 啊，这个函数刚讲过，在这里刚给大家讲过，也就是执行这个连接服务端的操作。说白了，loop 就是判断一下当前的客户端是不是跟服务端连接，如果是连接的话执行心跳机制，如果断开连接了就连接服务端。

好，讲到这里，我不知道有朋友会不会有一点疑惑，什么疑惑呢？我给大家提醒一下，请看这个连接服务端这个 connect 函数能看出有什么问题吗？我们看这个 connect 函数它所提供的参数只有一个，这个内容是什么呢？clientId。可是我们在之前课程中给大家讲过，当客户端尝试连接服务端的时候，它所发送这个 CONNECT 数据包它有三个必须要提供的信息，分别是 Client ID、Clean Session、Keep Alive。诶，那目前我们这个 ESP8266 客户端它这个程序中只提供一个 Client ID，那这个数据包中另外这个 Clean Session 以及 Keep Alive 这两个信息哪去了呢？事实上，在我们使用 PubSubClient 库中的这个 connect 函数如果只提供 Client ID 而不提供 Clean Session 也不提供 Keep Alive 的话，那么 PubSubClient 库将会使用一个默认的设置来对这个 Clean Session 以及 Keep Alive 进行设置。那么在默认情况下，这个 Clean Session 是 true，那么 Keep Alive 是 15 秒。注意，这里默认的 Keep Alive 是 15 秒，和我们刚才在 MQTT.fx 里填的 60 秒是不一样的，这一点您要清楚地知道，因为我们并没有在程序里做设置，所以只能是库给出的默认值。

好，那可能有朋友会说了，那我要想用 PubSubClient 库来设置这个 Clean Session 以及 Keep Alive 该怎么办呢？这个我在后续的课程中肯定是会给你做一个详细的讲解的。实际上在后续课程中，我不光会告诉你这个 Clean Session 和 Keep Alive 怎么设置，其他的比如 Username、Password 这些内容怎么设置，我都会在后续过程中给你做讲解。

好，那么到这里我们这个示例程序就给你讲解完毕了。接下来我们把这个示例程序把它编译并且上传给开发板，看看它运行状态。好了，程序已经成功上传给开发板了，接下来打开串口监视器，然后我们按开发板的复位键，我们看到开发板通过串口线告诉我它连接上 WiFi 了，并且它告诉我它已经成功连接到 MQTT 服务端了。这个等待的过程可能需要几秒钟的时间，请您耐心地多看一会儿串口监视器的输出，不要因为一时没有信息就以为程序没有运行。为什么这么说呢？我们在程序中，请看在我们程序中这个 connect mqtt_server 这个函数中，我们看到如果开发板成功地连接到了 MQTT 服务端以后，它将会通过串口监视器输出一系列信息，这一系列信息正是你在串口监视器里所看到这些信息。首先它告诉我它连上服务端了，然后它会告诉我它所连接的那个服务端的地址信息是什么，test 点 ran ye 杠 iot 点 net，这是"燃也物联"的这个服务端的地址。最后这里它会告诉我它的这个 Client ID 是什么，Client ID 请看这串字符，前面这个 ESP8266 横杠这些内容呢您和我是一样的，但后面请看这个 BC 冒号，这是我的开发板的这个 MAC 地址，它和您的这个设备的 MAC 地址肯定是不同的。这么做，我给大家说过，就是为了让我们这个开发板的它那个独有的 Client ID 一面呢和 MQTT 服务端向其他的客户端产生 ID 的冲突。

讲到这里我们这个客户端就已经成功地连接到服务端了。不过这段示例程序只是向你演示我们这个客户端它能够连接服务端，它还什么都不能做。在后续当中，我将会向你演示如何使用我们的客户端向服务器发送、接收信息。好了，那么这节课的内容到这里就结束了，非常非常感谢您，我们下节课再见。

---

*本文稿由 AI 从 B站 AI 字幕整理，如技术描述与原视频有出入，请以原视频为准。*
