# 3-1-2 基本操作

> 太极创客 · 零基础入门学用物联网 · MQTT 篇
> 时长 13:16

## 本节要点

- 申请到燃也物联社区版服务后，工作人员会通过微信通知；收到通知后登录燃也物联网站，在左上角的「社区版信息」菜单中查看服务信息
- 登录网站用的是网站注册的用户名和密码，与申请社区版服务时填写的用户名和密码是两回事，不要混淆
- 注意事项有三条：社区版仅限个人学习和测试使用、客户端发布的数据包不要超过 150 个字节、`keep alive` 参数要大于 10 秒钟
- 服务端基本信息：服务端地址 `iot.ranye-iot.net`，端口 1883
- 客户端连接信息表共有 8 组「用户名／密码／客户端 ID」组合，代表允许 8 台设备同时连接服务端
- 连接用户名规律是「网站注册用户名 + 下划线 + 序号」，客户端 ID 则在用户名基础上再附加 `_id`
- 专属主题级别：所有主题前面必须加上「用户名 + 斜杠」这一级别，才能正常订阅和发布
- 实测验证：三组信息中任意一项填错都无法连接，订阅别人的主题级别也既无法订阅也无法发布

## 正文

大家好。在上节课中，我引领着大家一起注册了燃也物联网站的用户。注册完用户以后，我们申请了一个燃也物联社区版服务。那么这个申请服务的目的，是为了我们后续的学习所使用。一旦我们成功地申请到了燃也物联的社区版服务以后，燃也物联的工作人员就会通过微信通知我们，告诉我们已经成功地获取到了这个社区版服务了。

当我们收到这样的通知以后，接下来我们就可以登录这个燃也物联网站来获得这个社区版服务的相关信息了。怎么来操作呢？接下来我给你演示一下。首先我们来到燃也物联网站，它的网址是 `www.ranye-iot.net`。然后我们来点击这个「登录」。这个登录的时候，我们要输入的是我们之前注册这个网站的时候所输入的这个用户名和密码。我这里输入的用户名是 `ltyuser`，那么密码是之前我注册这个网站的时候所使用的密码。注意，这是网站注册的时候所使用的用户名和密码，而不是申请的燃也物联社区版的用户名和密码，这个是有区别的。网站注册用户名、网站注册密码，点击登录。

那我们一旦成功登录到我们这个网站以后，接下来我们就可以看到左上角这里多了一个菜单，那写着「社区版信息」。它是什么内容呢？我们点击来看看。那么这个社区版信息的页面就会告诉我们如何来使用燃也物联社区版的一些基本信息、服务端的基本信息，还有客户端连接的信息等等等等。接下来我们会给您做一个详细的讲解。我们呢首先先从这个页面开始来看一看。

首先在这个社区版信息的一开始，这一系列的注意事项：这个社区版物联网服务仅能用于个人学习和测试使用，不得用于任何商业用途；另外，这个使用客户端发布信息的时候，数据包不要超过 150 个字节，否则的话服务端会拒绝接收这个数据包；另外，客户端 `keep alive` 参数的设置要大于十秒钟，每个客户端每秒钟发布一条消息，会让客户端的每秒钟发布的信息多一条。这些信息应该说都是比较容易理解的。假如你在看到这些信息有些困惑的话，那么说明可能您在我们之前这个台上讲的这个物联网教程上一部分，也就是在这个 MQTT 协议篇这里，您学得不是很扎实，才会导致你在看到这里这个说明信息的时候不是很确定它在说什么东西。如果是这样的话，我强烈建议您先看一看这个零基础入门学用物联网教程 MQTT 篇的这部分内容，然后再继续我们后续的教程学习，否则的话你会越学越糊涂。

我们还要言归正传，我们还回到我们这个社区版的信息中。那么这个是一系列的注意事项，我们要非常注意它，不然的话很有可能我们这个服务权限就会被暂停了。

那接下来这个信息就非常重要了，这后面的信息是关于燃也物联社区版的服务端连接的一些重要信息。首先服务端的基本信息，它的服务端地址是 `iot.ranye-iot.net`，那么 TCP 端口 1883，这些都比较容易理解。

接下来这个地方是客户端连接信息，这里它是提供了一系列的连接用户名、连接密码以及连接设备的 ID。它们是做什么的呢？我们看看。这个表格一共有八组信息，八组用户名、密码和客户端 ID 的组合。这些信息是当我们在使用客户端连接服务端的时候必须要正确输入的，也就是说用户名、设备 ID 以及密码。也就是说这个燃也物联社区版它允许我们使用八台设备同时来连接服务端。这八台设备，每一台设备要想连接服务端的话，它必须要正确地在它这个客户端里面设置用户名、密码或客户端 ID。如果这些信息不对的话，那么服务端将会拒绝这一台客户端的连接。那么通过这个机制，燃也物联的这个社区版就会保护我们个人的这个物联网通讯，只有正确输入这三组信息它才能正确连接。一共八台设备，这个就是这个表格里面的内容。

那么细心的朋友可能已经看到了，这个连接用户名，请看，它其实就是我们在注册这个网站的时候，请看 `ltyuser`，就是我注册网站的名字，后面加一个下划线再加个一，一到八，这用户名规律很好找。这个连接密码是我们在申请燃也物联社区版的时候我们填进去的一个密码，当时我是填写了一个特别简单的密码，是 `password`，英文 `p a s s w o r d`，`password`。那么这些信息已经是通过微信发送给我了。另外就是这个连接设备的 ID，连接设备 ID 也很好记，它首先就是我用户名下划线一，然后后面又接一条线，然后在这个 ID，也就是在这个用户名中的基础上多加了一个 `_id`。也就是说每台设备我们要输入用户名、密码和这个 ID，都输入正确以后才能够实现和服务端的连接。这个就是关于客户端连接信息的介绍。

你再往下看。专属主题。我们之前说过，因为这个社区版是专门属于我们个人使用的，那么既然专门属于个人使用的话，它是在这个服务端上专门开辟了这么一个级别，也就是说在这个 `ltyuser`，也就是我们的用户名下面的这个级别内容中，我们是可以任意使用的。我们是不能使用其他的、其他的级别中的内容，只能是在这个主题中前面先加上一个 `ltyuser` 再加斜线，说明了就是在这个级别下面的所有主题都可以用。这里有一系列示例，有像 `ltyuser/mytopic`、`ltyuser/kitchen/light`，`#` 我这个通配符，我用这样的通配符，那么这几个都是可以使用的。应该说燃也物联社区版它设置了这个个人主题级别的这个机制，正是为了保护我们的物联网通讯，只有我们正确输入了用户名密码以后才能用这些下面的主题。同理，我们也是不能够使用其他人的主题的，因为别人的这个主题内容也是同样的受到密码和这个 ID 的保护的。这个呢是燃也物联社区版它的一个设计机制，来保护我们这个物联网通讯。这是关于专属主题的内容。

我们接着往下看。那么社区版服务是有有效期的，到时候工作人员会通过微信联系我们进行免费的延期服务，免费延期服务。

那么讲到这里，关于这个燃也物联的这个社区版的信息，我要给你介绍的就是这些了。那下面我们来尝试着用 MQTT.fx 软件来尝试使用这个服务端的这个相关信息，我们来试着连一下，来进行一下发布订阅这么一个基本操作。

我们先把这个表格的信息和等一下要填的内容对应一下，这样您在自己操作的时候就不容易出错。表格中每一行给出三样东西：连接用户名、连接密码、连接设备 ID。连接用户名就是我们要填到 MQTT.fx 的「用户名」这个输入框里的内容；连接密码填到「密码」输入框；连接设备 ID 填到「客户端 ID」输入框。也就是说，表格里的三样信息，对应到软件里就是三个输入位置，一个都不能少，而且必须完全对应同一行的那一组，不能把第一行的用户名和第二行的密码搭配起来使用，那样服务端是认不出来的。这里我使用的是表格中的第一行，也就是序号为一的那一组信息。

首先我们打开 MQTT.fx 软件。打开 MQTT.fx 软件以后，接下来我来建立一个新的连接。怎么建立呢？我来点击，请看鼠标指的这个地方，点击一个齿轮这个图标，然后点击，就看这里左下角这个加号。那么在这里，我们就要给我们这些连接起个名字，叫什么名字呢？就叫 `taichi_maker_ranye_iot_test`，起这么个名字。接下来这里这个不用管它，默认就可以了。那下面这个地方，我们要填入燃也物联服务端的地址了，是什么还记得吗？`iot.ranye-iot.net`，好，端口 1883 不用管它了。

这里就要输入我们这台设备它的这个客户端 ID 了，这很关键，我们不能随便乱填。我们要填什么内容呢？我们现在就用它来做这个第一个设备，我们在这里就填入这个 ID。我来把这个复制，到我们这个 MQTT.fx 的设置地方去把它删掉，粘贴。好，这里这个是设备的 ID。请看设备的 ID，把它粘贴到这里来了。

那么接下来还有很关键的工作。现在这个 `keep alive` 时间它默认的是 60 秒，没有问题，因为燃也物联在这个地方说了，`keep alive` 时间是要十秒钟以上，60 秒完全符合这个要求的。另外，请看，我们接下来还要说一个非常重要的信息，是一个认证信息，这里需要填入用户名和密码，那只有正确填入用户名密码以后才能够连接到服务端。

我们这是第一个设备，我们就在这里用用户名 `ltyuser_1`。我在这里输入，好，输入用户名，密码呢大家还记得吗？`password p a s s w o r d`。然后点击「OK」。好，这样的话我们就设置完毕了。接下来我们要连接服务端了，「Connect」。好，这里看到了绿灯了，说明我们已经连接成功了。

那么下面我们来试试订阅一个主题。订阅什么主题呢？我们是必须要订阅我们这个用户名级别下面的主题，因此我们在这个订阅这个地方，请看鼠标指的地方，我们在订阅这个地方，我们要输入，请看 `ltyuser/`。这个级别下面的内容随便，我们就比如说就订阅一个叫做 `testtopic`，`test topic` 测试主题这么一个主题。我们点击订阅。好，已经成功订阅了。

那我们下面还用这个软件我们向这个主题随便发一个信息，这个主题信息复制到这里，`ltyuser/testtopic`，好，我们要发布这个信息，内容呢就是 `hello iot world`，「OK」，点击发布，我们来看看收没收到。「OK」，没问题，收到了。哦，我还拼错了，再来一次，改一改，好，`hello iot world`，再发一次。诶，你这回就没问题了。那通过刚才的这个测试，我们可以看到，利用燃也物联社区版的这个信息，我们是可以通过用户名、密码和 ID 这三组信息来保护我们的这个通讯了。

下面我们来试着，我们首先做一个实验。什么实验呢？我们试着订阅，比如说不是我们这个级别下面的内容，看能不能订阅。比如说我们就订阅一个就是名叫张三的、别人的这个级别下面的内容，好，我们试试看，订阅。诶，根本就无法订阅。那我们向这个主题上发布，发布，发布出去，根本就发不出去。那么这个就是我刚才说的，它这个社区版是利用我们的个人主题的这个级别来保护我们的这个通讯。因为我们是不可能向别人发布的，别人如果没有正确输入这个密码和用户名的话，他也是不能发布到我们主题的内容，也不能订阅我们主题的内容，这样的话就给我们这个物联网通信做到了一种隐私的保护。

好，那么我们再做个实验。我们这次断开连接，我们试着，请看刚才我们这地方说用户名是 `ltyuser_1`，我们下面把这个用户名把它改成乱七八糟的内容试试，看还能不能连接。来，我们再点击，啊，无法连接，说明我如果没有正确输入这个用户名是不能让我连接的。或者我比如说我改一下这个 ID，我现在这个用户名已经输入对了，密码也是正确的，我把这个 ID 改一改，ID 后面我也加一些乱七八糟的信息，点击「Connect」，同样的是无法连接的。

那么这个实验告诉我们，我们必须要正确输入这三组信息才能够连接。好，以上呢就是关于燃也物联社区版的这个基本使用方法了。那么在后续课程中，我们还会利用这些信息来做进一步的测试和操作。那么这节课内容到这里就结束了，非常非常感谢您，我们下节课再见。

---

*本文稿由 AI 从 B站 AI 字幕整理，如技术描述与原视频有出入，请以原视频为准。*
