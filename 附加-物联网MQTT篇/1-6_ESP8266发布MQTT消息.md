# 1-6 ESP8266 发布 MQTT 消息

> 太极创客 · 零基础入门学用物联网 · MQTT 篇
> 时长 17:17

## 本节要点

- 使用 PubSubClient 库编写 ESP8266 向 MQTT 服务端发布消息的示例程序，该程序在 1-4 连接服务端程序的基础上扩展而来
- 通过串口监视器观察开发板的运行状态：连接 WiFi、连接服务端、显示客户端 ID（ESP8266 加 MAC 地址）、不断发布 hello world 加数字
- 使用 MQTT.fx 订阅开发板发布的主题，验证开发板已成功向燃野物联服务端发布信息
- 逐段解析示例程序：引入三个头文件、设置 WiFi 信息与服务端地址、建立 Ticker、WiFiClient、PubSubClient 对象
- 使用 Ticker 库配合计数变量实现定时任务，每隔三秒钟调用一次发布函数
- 掌握 publishMQTTMsg 函数的实现要点：静态变量自加、字符串转字符数组、发布返回值判断与串口反馈

## 正文

大家好，欢迎来到"零基础入门学用物联网"教程 MQTT 篇。这节课我们一起来学习如何使用 ESP8266 开发板来发布 MQTT 消息。首先我们先打开这节课的讲义内容，是这里 1-6 ESP8266 发布 MQTT 消息。

既然是要讲解如何使用 ESP8266 来发布 MQTT 消息，最重要的还是要给大家讲解这个示例程序了。那么这个示例程序，同样的还是使用了 PubSubClient 库。关于这个库如何来下载，我们在之前课程中给大家讲过，在 1-4 这节课程中，我们给大家详细讲解了如何来获取一个库。好，如果你还没有看过这节课中的内容的话，那么我建议你先来看看这节课里的内容。因为我们在这节课中所使用的这个程序，将会是在 1-4 的这个程序基础之上添加内容。1-4 这节课里面，我们给大家讲解了如何使用 ESP8266 来连接 MQTT 服务端，那么这节课我们接下来给大家讲解如何来使用 ESP8266 向 MQTT 服务端发布消息。

好，那下面我们就把这段示例程序复制并且粘贴到 Arduino IDE 中，我们来看看这段示例程序的具体内容。

### 先看程序的工作状态

好了，现在程序已经粘贴到 Arduino IDE 中了。接下来我们来看看这段示例程序的主要内容。不过在学习这段示例程序的主要内容之前，我们先来看看这段程序它的工作状态。为了节省您的时间，我已经预先把这个程序编译并且上传到我面前的 ESP8266 开发板了。接下来我们来打开串口监视器。好，此时打开串口监视器，我们会看到，我面前的 ESP8266 开发板它在不断地通过串口监视器在输出内容。

好，输出内容是什么呢？看着还挺复杂的，没关系，我慢慢给你讲。我们先关闭这个自动滚屏，为了便于观察，然后我们来到这个信息内容的开始。首先开发板告诉我它连上 WiFi 了，然后他会告诉我，当前他已经连上 MQTT 服务端了，那么这个连接的 MQTT 服务端的地址是 test.bemfa.com 燃野物联的服务端地址。然后他就告诉我他当前的客户端 ID 是什么，是这里后面的这串字符。这串字符前面的是 ESP8266，横杠后面这个内容是我们 ESP8266 开发板它的 MAC 地址。如果您在使用这段示例程序把它上传到您的开发板的时候，您将会看到您的客户端 ID 和我这里是不同的，因为不同的开发板它的 MAC 地址是不一样的，所以后面这个地方看到的应该也是不同的。

我们来看看这串客户端 ID，它的前半部分是 ESP8266 这个固定的字符，横杠的后面接着就是这一块开发板自己的 MAC 地址。也就是说，这个客户端 ID 是由"开发板的型号"加上"这一块板子独一无二的硬件地址"拼起来的。这样拼出来的客户端 ID 有几个好处：第一，一看就知道这个客户端是一块 ESP8266 开发板，而不是电脑上的 MQTT.fx 软件；第二，因为 MAC 地址是每一块板子出厂时就固化在芯片里的，不会重复，所以就算您有好几块 ESP8266 同时接到同一个服务端上，它们的客户端 ID 也不会撞车。这一点很重要，因为服务端需要凭客户端 ID 来区分不同的客户端，一旦有两个客户端用了同一个 ID，服务端就会把其中一个踢下线，两个客户端就会反复地互相抢连接，谁都工作不正常。

那么为什么这么做呢？我们这节课程给大家讲过，连接同一个服务端不能使用相同的客户端 ID，否则会发生冲突。这里为什么我们把 ESP8266 的 MAC 地址把它放到后面，就是为了保证每个开发板的客户端 ID 都是唯一的。

接着我们来看，接着看后面这几行内容，这四行内容是 ESP8266 开发板它在告诉我们，他在向一个主题发布内容。好，是哪个主题呢？应该是这里这个主题，后面这里又是 ESP8266 的 MAC 地址了。他在不断地向这个主题发布内容。那么发布的内容是什么呢？请看，是后面 hello world 后面跟一个数字 0。紧接着他又告诉我们，还是在向这个主题发布 hello world 1、hello world 2，不断的数字在增加。那么这个很简单，这个客户端就是在告诉我们，他在不断地向这个主题发布 hello world 后面跟的数字。

### 用 MQTT.fx 接收开发板发布的消息

那么既然是在向这个主题发布内容，那自然我们是可以收到这个内容的了。怎么能够收到它呢？我们可以使用 MQTT.fx 这款软件来收到 ESP8266 客户端所发布的内容。怎么来收到呢？我们首先连接燃野物联的服务端，为什么呢？因为我们这个 ESP8266 客户端连接的就是燃野物联的服务端。好，那我们接下来首先让它连上燃野物联的服务端。我们之前的课程中给大家讲过，这个"太极创客 MQTT 教程示例"它所使用的正是燃野物联的服务端。

这里再顺便说一下，为什么我们要用 MQTT.fx 这个电脑上的软件来接收开发板发布的消息呢？因为通过这种方式，我们可以非常直观地看到：开发板这边发出去的信息，到底有没有真正到达服务端，又是以什么内容、什么主题到达的。如果开发板这边在串口监视器上显示发布成功了，电脑这边又收到了同样的内容，那就可以百分之百确认我们的程序是正常工作的。这种"一边发、一边收"的验证方法，在我们后面调试 ESP8266 的程序时会经常用到，希望您能掌握。

好，那么接下来我们就点击 Connect，当我们看到这里这个绿灯的时候，我们可以知道软件已经连上服务端了。接下来我们就要订阅这个 ESP8266 开发板所发布的这个主题了。我来把这个地方高亮，然后按一下这个 Ctrl+C，那么 Ctrl+C 就可以把这个主题名字复制到我的剪贴板中。

另外我在这儿再提醒一下，如果您在使用您的示例程序的时候，这里这个订阅主题的名字是和我这里不一样的，这很正常。因为后面这一串这是我的 ESP8266 开发板的 MAC 地址，我这么做的目的，也是为了让不同的朋友在使用同一个服务端同时学习这个教程的时候，不要同时向同一个主题发布内容，否则会造成混乱。所以我在这里就把不同的开发板的这个 MAC 地址放在了这个主题的后面，也就是说我们不同朋友他发布消息的这个主题名字是不一样的，为了避免混乱。说这么多，我们呢已经把它复制好了，这个主题名字已经复制好了。那接下来我就来把这个主题的名字把它粘贴到这里，好，接下来点击 Subscribe。

我点击 Subscribe 以后可以看到，当前我们这个 MQTT.fx 软件它在不断地接收到信息，这个信息自然是来自于 ESP8266 开发板了。我们打开这个自动滚屏，调整一下这个位置。没问题，我们可以看到当前我们这个 ESP8266 的开发板所发出的内容和 MQTT.fx 软件所接收到的内容几乎是同步的。这个实验就可以很好地证明：目前我们的 ESP8266 开发板已经成功地把信息发布到燃野物联的服务端了，并且我们这个 MQTT.fx 软件也成功地从燃野物联服务端接收到了信息内容。

### 程序解析：头文件与全局对象

好，那接下来我们就来看一看这段示例程序它的主要内容了。首先我们来到程序刚刚开始的地方，请看这里有三个 #include。首先这个程序将要使用 ESP8266WiFi 库，这个库我们见了太多次了，ESP8266 联网就要用到这个库。第二个是 PubSubClient 库，这个也没什么好说的，我们这个程序要实现 MQTT 通讯就要用这个库。第三个是 Ticker 库，这个库呢，我不知道有朋友认不认识，我们在之前课程中给大家介绍过，通过这个库，我们的 ESP8266 可以定时执行任务。

关于这个库，我们在之前课程中给大家介绍过。假如您对我们以前课程中的内容渐渐淡忘了，或者您对这个库的使用方法不太了解的话，您可以通过太极创客网站的首页，在首页中有个"开发物联网"的链接，我们点击它，进入这个"开发物联网"的链接以后，我们可以看到中间这里就是"开发物联网资料"这个链接，进入这个页面。好，在这个页面中请看这里有一个 ESP8266 NodeMCU 应用技巧这么一个链接，进入这个页面。好，在这个页面中，我们给您列出了一系列的 ESP8266 的应用技巧，其中就包括我们这里给您介绍的这个 Ticker。好，点击它就可以进入这个 Ticker 库的使用介绍页面了。另外，在这个 ESP8266 的应用技巧的这个页面中，还有一系列其他的应用技巧，比如说如何使用 WiFiManager 库来实现这个自动配网，以及 ESP8266 的 OTA 功能，这些您如果感兴趣的话，可以到这里来看一看。

好，我们还要回到我们程序中来。我刚才说的这个库是可以让我们 ESP8266 定时执行任务，具体这个如何实现的话，后面我们可以看到。好，我们继续往下看。这里是我们 ESP8266 联网的信息，联网的名称以及联网密码，您需要根据您的情况进行实际的调整。接下来这里这个地方，存储的是即将连接的 MQTT 服务端的地址，也就是燃野物联的服务端地址，这些我们之前都见过，不在这浪费时间了，我们继续往下看。

好，在这里我们建立一个 Ticker 库的对象，这个刚刚说过，是用来定时执行任务的。然后我们建立一个 WiFiClient 对象，这是用来联网的。然后建立一个 PubSubClient 对象，这个是用来实现 MQTT 通信用的。这些内容我们之前也都见过，不在这浪费时间了，我们继续往下看。

我们稍微解释一下这三个对象各自的分工，这样您看程序的时候就不容易搞混了。Ticker 对象负责的是"什么时候做事"，它管的是时间，就是定时去触发某一件任务；WiFiClient 对象负责的是"物理上把网连起来"，它是 ESP8266 和网络之间的一个通道；而 PubSubClient 对象负责的才是真正的 MQTT 通讯，也就是往里发消息、收消息、连服务端这些事情。而且您注意，PubSubClient 是架设在 WiFiClient 之上的，也就是说它必须借助 WiFiClient 这条网络通道才能和服务端说话。所以在程序里，我们一定要先把 WiFiClient 这个对象准备好，再把它交给 PubSubClient 去使用。这三个对象各司其职，一段完整的 MQTT 发布程序其实就是靠它们配合起来的。

### 程序解析：setup 函数

好，接下来我们建立一个整数型的变量，这个整数型变量是用来计数用的，怎么计数呢？后面你就可以看到了。我们继续往下看，我们来到 setup 函数。首先启动串口通讯，然后让 ESP8266 设置为无线终端模式，然后请看执行这个函数，这个函数是连接 WiFi 使用的，这些我们之前都讲过，不浪费时间了。

接下来这条语句，这条语句调用了 PubSubClient 库中的 setServer 这个函数，具体使用这个连接服务器的信息内容，我们之前在程序中设置过，这里使用这个燃野物联的服务器地址放在这里，那么连接的端口是 1883，这个之前给大家讲过。然后这个函数，这个函数是实际上让 ESP8266 连接 MQTT 服务端，这个函数的内容我们在之前的课程中也给大家讲过了，在我们给大家讲这个 MQTT 服务端连接这个实现的这个教程中，曾经给大家讲过这个参数内容，都在这里，这个内容和之前的课程中是完全一样的，我在这里就不再给大家重复了。好，那么通过这个函数，我们的 ESP8266 将会实现和 MQTT 服务端的连接。

### 程序解析：用 Ticker 实现定时发布

连接好以后请看这里，这个是用 Ticker 库中的这个 attach 函数，让我们的 ESP8266 每隔一秒钟就执行一下 tickerCount 这么一个函数。tickerCount 这个函数在哪呢？每一秒钟执行一次的这个参数就在这里，tickerCount 每隔一秒钟它就会让 ESP8266 把这个 count 数值自加一。count 我刚才说，在程序刚刚开始这里有一个建立了一个整数型的变量，那么我刚才跟大家说过，这个整数型变量就是用来计数用的。

我们还回到这里，让 ESP8266 每隔一秒钟就让它执行一下这个 tickerCount，每执行一次 tickerCount，count 这个值就会增加一，为什么要自加一呢？是有原因的。在 loop 函数中请看，loop 函数中我们首先来判断一下 ESP8266 是不是连接到了 MQTT 服务端了，如果成功连接到的话，那么就让这个 ESP8266 判断一下当前这个 count 这个计数变化有没有达到数值三。如果达到数值三了，那么我们就让它执行这个逻辑判断中的这些内容。那么通过这个机制，我们的 ESP8266 如果它在这个 MQTT 客户端成功连接上以后，那么它每隔三秒钟就会执行一下这个逻辑判断中的内容。为什么？因为我们用这个 count 变量，我们让它判断一下是不是等于三，每达到三的时候，我们将它执行一次这个叫做 pubMQTTMsg 的这么一个函数，每三秒钟执行一次，这个函数执行完以后把 count 这个数值清零，以便下一次然后它这个 count 函数能够再一次达到三，再次执行。每隔三秒钟执行一下这个函数的话，那么一会我们来看看这个函数具体内容是什么。

### 程序解析：publishMQTTMsg 函数

我们看看这个函数在哪里呢？pubMQTTMsg，在这里。pub 呢是 publish，也就是发布的缩写，MQTT 没什么好说的，那就是 MQTT，msg 呢是信息的意思。换句话说，这个函数名字就是"发布 MQTT 信息"的意思。具体怎么实现的发布呢？好，我们看到在这个函数中，有一系列的程序内容，还挺丰富的，看着挺吓人的，哈哈，没事，我慢慢跟你讲。

首先我们来看，好，这个函数刚开始这里，建立了一个整数型的变量，这个整数型变量的作用是 ESP8266 他在向服务端发布这个信息的时候，请看，他已经发布了 hello world 后面跟了一个数字 92、93，这个数字就是来自于这个整数型变量的值。值得一提的是，这个整数型变量我们把它设置为静态的 static，这么做的目的就是每次调用这个函数的时候，让这个数值能够保持，这样才能够实现这个数值自动增加这么一个效果。好，具体实现方法一会就可以看到，这个就是 value 这个数值的作用。

我们继续往下看，我们看到这里，这些语句非常关键，我们要仔细看一看了。首先我们这里建立了一个字符串型的变量，这个字符串变量所存储的内容是 topicString，topic 主题，String 字符串，主题字符串，那自然它这里面所存储的内容正是我们即将发布的这个主题的名字。好，后面跟的是开发板的 MAC 地址，没错，我们在串口通讯中可以看到，这里我发布的正是这么一个主题。

那么建立这个主题字符串以后，接着请看这两条语句，这两条语句是把这个字符串的变量做了一个转换，把它转换成一个字符数组。为什么要转换成字符数组呢？是因为我们这个 PubSubClient 库，他在执行发布的这个 publish 这个函数的时候，所能够接受的参数必须是一个字符数组型的一个参数，我们不能使用一个字符串，能使用字符串必须要使用一个字符数组。所以我们才把这个字符串通过这两条语句做了一个格式的转换，把字符串转换成字符数组，转换成字符数组一个名字叫 pubTopic，这个就是这个字符数组的名字了。这个 pubTopic 这个字符数组就作为这个 publish 函数的第一个参数来把它放在这里。那这个 publish 函数就是用来实现 ESP8266 向 MQTT 服务端发布信息用的。这个就是第一个参数，pubTopic 也就是发布主题的名字。

同样的，井号后面这里的这个操作也很像。这里我们是又建立了一个字符串型的变量，这个变量里面存储内容请看，hello world 我们加上一个什么呢？加上你看这里这个 value 加加，就实现了这个咱们这个函数刚开始地方让这个数值自加。好，我们回到这里，好，我们建立这个字符串，让它附参数的用的是 hello world 后面加上一个数值，然后再并且把这个字符串也通过这两条语句把它转换成字符数组，好，然后用字符数组的名字叫 pubMsg，这个 pubMsg 就放在后面请看，这个 publish 参数中作为第二个参数。第一个参数 pubTopic 是发布主题名字，第二个参数是发布信息内容是什么。这两个参数都有了以后，我们就可以调用 publish 这么一个函数，这 publish 我们讲了太多次了，就是发布信息的意思，调用这个函数就可以实现向 MQTT 服务端发布信息了。

### 程序解析：发布结果的判断

那么这个发布信息以后，它呢是有一个返回值的，通过这个返回值我们来判断一下发布的信息是不是成功了。如果成功了的话，它将继续执行后面的内容；如果失败了的话，那么它将执行这类内容。我们看，如果发布信息成功的话，它将会通过串口消息告诉我们，目前的这个 ESP8266 已经向这个主题发布了，这个主题是什么呢？这主题正是我们在串口监视器中可以看到的，看到这里这个主题的名字，正是通过这条语句实现的。好，发布的信息内容呢，也是串口监视器我们可以看到，后面这里 hello world 后面加个数字，通过这里，通过这个软件串口监视器告诉我们发布的具体内容是什么。这是发布成功了。

那如果发布失败了的话呢，如果反复失败的话，就通过串口来告诉您 message publish failed 的信息，也就是信息发布失败，就这么简单。我们这个函数是讲完了，通过这一系列操作，我们就可以实现 ESP8266 向 MQTT 服务端发布信息了。

### 程序解析：loop 函数中的心跳机制

好，那我们还要回到 loop 函数中调用这个 pubMQTTMsg 这个函数的地方，回到 loop 函数中，哪儿调用这个函数呢？好，在这里。现在我们这个 loop 中，让 ESP8266 检查一下它是不是成功连接到 MQTT 服务端，成功的话，让它看看是不是达到三秒钟了，如果达到三秒钟的话，那么它将会执行这个函数。通过这个，将会向服务端发布信息，这就是你可以看到刚才我们在演示中看到，每隔三秒钟我们的 ESP8266 向服务端发布一条信息，这是逻辑判断。

那么除了要执行这个操作以外，我们还有一个很重要的操作是什么呢？这里这个 loop 函数，这个 loop 函数我们之前给大家讲过，它是用来保持心跳机制的，这是成功连接到 MQTT 服务端的时候执行这一系列操作，发布信息、保持心跳。那么如果连接失败的话呢，那么就让 ESP8266 尝试连接 MQTT 服务端。好，这些内容呢，和之前的程序是完全一样的，所以就不在这浪费时间了。到这里我们这程序就讲解完毕了。

好了，关于如何使用 ESP8266 开发板向 MQTT 服务端发布信息的操作，我们这课先给你介绍到这里。后续我们会给您进一步介绍这个操作的内容，到这里就结束了，非常感谢您，我们下节课再见。

---

*本文稿由 AI 从 B站 AI 字幕整理，如技术描述与原视频有出入，请以原视频为准。*
