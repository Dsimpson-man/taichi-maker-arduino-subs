# 1-9 自我测试

> 太极创客 · 零基础入门学用物联网 · MQTT 篇
> 时长 11:05

## 本节要点

- 回顾已学内容：MQTT 通讯基本原理、客户端连接服务端的 CONNECT 报文、客户端 ID / Clean Session / Keep Alive 等关键信息
- 回顾使用 MQTT.fx 与 ESP8266 开发板连接服务端、发布与订阅消息的操作，以及 MQTT 主题的形式、分级和通配符知识
- 观察测试演示：开发板定时向一个主题发布消息，按下按键发布 off，松开按键发布 on，用按键控制发布的内容
- 使用 MQTT.fx 订阅开发板发布的主题，验证开发板向服务端发布消息的功能
- 使用 MQTT.fx 向开发板订阅的另一主题发布消息（把主题中的 pub 改成 sub），验证开发板接收消息的功能
- 通过自己动手编写这段示例程序，检验前面所学知识是否掌握扎实

## 正文

大家好，欢迎收看"零基础入门学用物联网"教程。通过之前几节课的学习，我们已经对 MQTT 协议具备了一定的认识。那么这节课我们将要对我们之前所学的课程内容做一个简单的总结，另外我们要对我们目前所学的这个知识的掌握情况进行一个自我测试。

### 前面学过的内容回顾

到目前为止，我们学习了 MQTT 的通讯基本原理，以及如何让我们的客户端来对 MQTT 服务端实现连接。在连接过程中，我们是用客户端向服务端发布相应的数据包，这个数据包它们的官方名称叫做报文。我们掌握了当客户端想连接服务端的时候，这个报文里面的基本信息，这些基本信息非常非常重要。在我们这一部分的教程中，我们对客户端 ID、Clean Session 以及 Keep Alive 做了一个基本的讲解。

接下来我们又学习了如何使用 MQTT.fx 软件以及 ESP8266 开发板来连接 MQTT 服务端。连接上 MQTT 服务端以后，我们又学习了如何来使用客户端发布和订阅 MQTT 消息。当 MQTT 客户端向发布消息的时候，它将会使用主题这个概念向某一个主题来发布消息；同样的，当 MQTT 客户端想要收取某些消息的时候，它也要先订阅这些主题，然后它才能收到这些主题的消息。

了解了 MQTT 客户端是如何来实现发布和订阅消息以后，接下来我们又学习了 MQTT 主题方面的知识。在这一课里，我们学习了 MQTT 主题的基本形式，其基本形式就是一个字符串；以及我们学习了 MQTT 主题是如何来分级的，是通过这个斜杠来进行分级的；以及这个 MQTT 主题的通配符如何使用，有单级通配符和多级通配符两种通配符；还有我们在使用 MQTT 主题的时候应该注意哪些地方。通过这些学习，我们目前已经可以实现最基本的 MQTT 发布和收取消息这样的操作了。

### 自我测试：演示说明

那这节课我们来对我们目前所学的知识做自我测试，我们看看目前这些知识所掌握的情况如何。那如何进行自我测试呢？首先你先来看我一个演示，看看这个演示的内容是什么，然后再告诉您这个测试的内容是什么。好，那接下来我们这个演示开始。

首先我告诉您，在我们面前有一个 ESP8266 开发板，并且我已经写好一段示例程序了，把这个程序已经上传到开发板中了。那这段示例程序的控制下，我们的开发板它将会连接 MQTT 服务端，并且连接上服务端以后，它会向服务端上面的一个主题不断地发布消息。来最大化并且关闭自动滚屏，我们来看。我们看到开发板在不断地向这个主题，这里数个高亮这个地方，这是包含 pub 后面这是开发板的 MAC 地址，他在不断地向这个主题发布消息，消息内容是在不断地发布这个 on 这么一个消息。好，我来打开自动滚屏，你看到每隔几秒钟就发布一个消息，你看每隔几秒钟发布一个消息。

那这个消息不是说只能发布这个 on，它还能发别的内容，怎么样让它发别的内容呢？这就要我用手来按一下开发板上的按键了。那么接下来请注意屏幕，我将用我的手按下开关按键。好，我按下按键，我们看到，等我按下按键以后，开发板上这个信息变成 off 了。好，松开按键，我们可以看到，当我手指离开按键以后，它就会恢复成 on。按下就变成 off，它向这个主题发布的这个主题名字都是同一个，都是同样的主题。换句话说，通过我这个手指按下和不按下这个按键，我们可以控制开发板它所发布的信息内容。

### 自我测试：用 MQTT.fx 接收开发板消息

那么既然开发板向这个主题发布信息了，那接下来我们来尝试来接收这个信息。我们将要使用 MQTT.fx 软件来接收 ESP8266 所发布的消息。那首先我要把这个即将订阅的主题把它复制到剪贴板中，然后我来进入 MQTT.fx 软件，连上服务端，并且把这个即将订阅的消息的主题粘贴到这里，然后点击订阅。

这里要注意的是，我们要订阅的主题名称必须和开发板发布消息时所用的主题名称完全一致，一个字符都不能差。您看这个主题前面一段是大家共有的，后面这一段是这块开发板自己的 MAC 地址，所以您在做这个测试的时候，一定要从您自己的串口监视器里把主题名字复制出来，不能照抄我这里看到的。这一点我们在前面的课程里反复强调过，因为每个人的开发板 MAC 地址都不一样，主题自然也就不一样，照抄别人的主题是绝对收不到消息的。

好，你可以看到，MQTT.fx 这边点击订阅以后，MQTT.fx 已经不断在接收到 ESP8266 所发布的消息了，信息是 on。

那接下来请注意，我们把这个格式调整一下，这样大家可以看得清楚一点。好，这里是我们 ESP8266 开发板的发布的消息内容，这里是我们的 MQTT.fx 接收的内容。接下来我来按一下开发板上的按键，我来按一下。好，没问题，我们可以看到开发板发布的是变成了 off 了，同时我们看到这边接收的也变成 off 了。好，我们来松开按键，同样的，这边发布是 on，这里回复成 on 了。通过这个演示你可以看到，我们的 MQTT.fx 这个软件已经可以成功地订阅到 ESP8266 所发布的消息了。

### 自我测试：用 MQTT.fx 向开发板发布消息

那接下来我们再做另外一个测试。同样的，我们用这个 MQTT.fx 软件，我们将向某一主题来发布消息，那么是哪个主题呢？请看，我们把刚才这个主题里面的 pub 改成 sub，我们把这个 pub 改成 sub，注意，这里变成 sub 了。好，那么通过我这里所发布的消息，我们的 ESP8266 开发板还是可以接收到的，因为什么呢？因为我们这个示例程序我们已经让这个开发板订阅这个主题了。那接下来我们来看看是不是可以成功地接收到。好，我们把这个格式再调整一下，然后我们看得更清楚一点，有点费劲。好，行，先这样子吧。

接下来请看，我向这里输入我们将要去发布的信息内容，比如输入 hello。请注意，我将要点击这个 Publish 了。点击，我们可以看到，刚我点击以后，我们这里就出现了新的内容了，关闭串口滚屏。好，请看，这是刚刚我们说到的，这里这是刚刚收到的内容，就是收到的这个主题是哪一个，以及还有主题的内容和这个信息的长度是 18 个字节，没有问题。我们再打一个新的内容，我们来看一看。好，没问题，就在这里 this is from mqtt fx，长度是 19。

因为刚才我这演示大家可以看到，我们已经是可以使用 MQTT.fx 软件向某一主题发布信息，那么发布这个信息，我们 ESP8266 将会接收到这个信息。同时我们 ESP8266 也在定时地向某一个主题发布信息，发布信息内容是用开发板上的一个按键的状态，通过按下和松开这个按键，我们就可以控制这个开发板所发布的信息内容。好，这个就是我们这个演示了。

### 自我测试：请您自己动手实现

我希望您能够尝试着自己来写一段 ESP8266 的控制程序，那么这个控制程序就可以实现刚才我演示您看到的这个状态。那么如果您可以自己实现的话，太好了。如果您在这个写程序的过程中感觉到有点困难的话，没有关系，我们这里把这段示例程序提供给您了，就在我们这一节的后面。这套示例程序正是刚刚你可以看到这个演示它所控制 ESP8266 的这个示例程序。那么通过这段程序，我们的 ESP8266 在同时地发布消息以及订阅消息，这个就是这个示例程序它的主要功能。

好了，那么关于我们这节课就要给你讲解的就是这些了。因为我们这示例程序中所涉及的知识内容都在我们之前的课程中给你讲解了。如果您对我们这些程序哪些地方感觉有点困惑的话，那可能您之前的课程还没有学习得非常扎实，我建议您先把这些知识内容先搞清楚，我们这套示例程序具体是怎么实现的，每一个细节都搞明白，然后再来继续后面课程的学习。

在接下来的学习中，我们将不再局限于 MQTT 的基本通讯了，我们将会学习更多的 MQTT 的高级功能，比如像服务质量，这个我们之前跟大家说过很多次，服务质量，以及像如何来实现保留消息、心跳机制、遗嘱这些内容。好，这个内容到这里就结束了，期待在后续的课程中与你相见，非常感谢您，我们下节课再见。

---

*本文稿由 AI 从 B站 AI 字幕整理，如技术描述与原视频有出入，请以原视频为准。*
