# 1-8 Arduino程序 - 程序函数1

> 系列：零基础入门学用 Arduino（第一部分）｜ UP主：太极创客
> 时长：7 分 49 秒

## 正文

大家好。首先我给大家介绍一下程序函数的基本概念，然后我会给大家讲解一下什么是程序函数的参数，最后我会给大家演示如何在程序中使用函数。请打开您的 Blink 这条示例程序，我们往下看。

请看这里，在这节课以前，我们反复地讲述了如何使用 `delay` 这个指令，相信您对它也已经有一个认识了。它的目的就是让程序等待一段时间，等待多久呢？只要在这里写出来就可以了。而事实上，为了让 Arduino 能够实现这个等待 1 毫秒的功能，它背后是有很大量的工作的，比如它需要涉及到很多关于对硬件的知识的理解。为了便于我们用户来使用，Arduino 公司已经把这个工作做好了，我们现在只要做的就只要写出这样一条指令就够了。事实上这种叫做调用函数。

什么叫调用函数呢？就是 `delay` 本身是一个 Arduino 公司的工作人员已经写好的一个函数，我们不用去管它具体里边写的内容是什么，我们只要知道：首先我们要知道函数的名称是什么，然后我们还要知道我们在使用这个函数的时候，我们要给这个函数一个什么样的参数。比如 `delay`，我们要给它一个参数，这个参数是时间长度。同时我们在调用函数的时候，我们还要知道这个函数它的返回值是什么。这个概念我现在先留在这里，给大家留一个小扣子，那么后面我们将来再继续讲函数的时候，我会跟大家再做一个详细的介绍。

同时我们再看这个 `delay`，我们现在再重新看这个 `delay` 这个函数，它只需要我们给它一个参数。你看我们上面这个 `digitalWrite`，它也是一个 Arduino 公司已经预先写好的一个函数，我们在调用它的时候，请看这圆括号里面，它事实上它是有两个参数的，这两个参数中间用一个逗号进行分隔。而且您在使用 Arduino 的过程中会发现，有些函数你在调用它的时候是可以不用给它任何参数的，那这个时候我们怎么做呢？我们就给它一个空号，一定要给一个圆括号，那么里面什么内容都不写就可以了。

但是对于我们每调用一个函数的时候，我们一定要记住它有一个固定的格式，什么函数需要多少个参数，这是固定的。假如比如说我们这个 `delay`，我们现在没有给它一个时间参数，那好，我们现在来编译看看结果是什么。OK，很明显这里错了，为什么？因为我没有给它一个参数。那么我们现在如果要是我们给它多一个参数，比如说我们现在给它 100，用一个逗号，我们再又给了它一个参数 110，我们给了它两个参数，我们在编译的时候我们看到什么样子呢？同样还是报错。为什么呢？这个 `delay` 这个函数它只用一个参数就够了，你多给它它不明白。是为什么我们只能按照 `delay` 这个函数它的要求给它一个参数，才能够正常的工作。

那你可能会问，究竟 Arduino 公司为我们写了多少个函数？您可以到 Arduino 公司的官网 `www.arduino.cc` 这个网址，请看 learning 下面有一个 reference，好，我们来点击它，我们往下看。请看这里 functions，functions 就是函数的英文名称，在这一列里面列出了所有的 Arduino 公司为我们已经预先写好了、所以我们可以调用的函数，它每一个函数都会实现一个功能。那我们来找一找 `delay` 在什么地方？在这里，在 time 时间 delay，点击我们看一下它说明是什么。好，这个页面里面就是关于 `delay` 这个函数它的一些介绍说明了，如果您英文好的话，您可以通过这里来了解它的相关说明。假如你感觉读英文有点吃力的话，那么您可以到我们太极创客的官方网站 `www.taichi-maker.com` 这个网站来，然后点击这个参考资料下面 Arduino 编程，往下看最后面函数这一列，我们已经把所有的 Arduino 这个参考资料都翻译成中文了，`delay` 也不例外，请看这里就有它的中文说明了。OK，`delay` 函数可以用于暂停程序运行，暂停时间可以由 `delay` 函数的参数进行控制，单位是毫秒，1ms。

这节课的知识到这儿就结束了，接下来我想跟大家留一个小作业，请大家来到我们官方网站，请留意在这个时间这个部分里面还有一个函数，它的名字叫什么呢？叫做 `delayMicroseconds` 这个函数，它的作用是什么、怎么用，我想让大家自己尝试着去试一试。

我们点进去看一看，当我们进入到 `delayMicroseconds` 这个函数页面以后，我们往下看说明：`delayMicroseconds` 与 `delay` 函数都可用于暂停程序运行。好，注意它的作用是暂停程序运行，不同的是 `delayMicroseconds` 的参数单位是微秒。我们还记得 `delay` 它的参数单位是什么呢？好，我们看一下 `delay` 这个函数它的页面，单位是毫秒，OK，找到差距了。

那么其他的呢？我们再看一看，我们再回到 `delayMicroseconds` 这个页面，我们再往下看它的语法是什么呢？是 `delayMicroseconds`，也就这个功能函数的名称，接下来我们要写一个圆括号是它的参数，参数是什么呢？好，参数是暂停时间，这个时间单位是什么呢？是微秒，也就是说我们在这要告诉 Arduino 你要停多少微秒。好吧，这是一个很短很短的时间了。嗯，返回值呢是 void，也就是说 Arduino 对这个函数它是没有返回值的，没关系，你要不理解什么是返回值，现在还不用担心，我们后边还会讲。

接下来这还有一个实例程序，您可以把这段程序复制，然后粘贴到您的 Arduino IDE 里面，然后运行它，看看它的运行情况是什么样子的，并且根据它的运行情况。这节课的内容就到这里了，谢谢各位，再见。

---

## 本节要点

- 调用函数就是使用别人（如 Arduino 公司）预先写好的函数，使用者无需关心内部实现，只需了解函数的名称、参数和返回值。
- 调用函数必须遵守固定的格式：函数需要几个参数就传几个参数，多传或少传都会导致编译报错。
- 有些函数调用时不需要任何参数，此时仍需写一对空的圆括号 `()`。
- `delay()` 只需一个参数（时间长度，单位为毫秒），参数用圆括号括起；多参数函数如 `digitalWrite()` 则用逗号分隔两个参数。
- 可通过 Arduino 官网 `www.arduino.cc` 的 Learning → Reference 页面的 functions 列表，查询官方提供的全部函数。
- 太极创客官网 `www.taichi-maker.com` 的「参考资料 → Arduino 编程 → 函数」栏目提供了全部 Arduino 参考资料的中文翻译。
- `delayMicroseconds()` 与 `delay()` 都用于暂停程序运行，区别在于前者参数单位是微秒，后者是毫秒。
- `delayMicroseconds()` 的返回值是 void，即该函数没有返回值。
