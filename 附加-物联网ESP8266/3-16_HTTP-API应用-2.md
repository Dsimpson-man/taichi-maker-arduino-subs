# 3-16 HTTP API 应用-2

> 系列：零基础入门学用物联网（基础知识篇）｜ UP主：太极创客
> 时长：35 分 34 秒

## 正文

以上呢就是如何使用 ESP8266 通过 HTTP API 来获取实时天气信息的操作讲解。我在之前的课程中给大家讲过，我们这个页面中一共三个实例程序，刚才给大家的是第一个实例程序，那接下来我们来看一看如何使用 ESP8266 来获取天气预报的信息。这个获取天气预报信息中，天气、降水概率等等等等，一共我们可以获取到三天的天气预报信息，也就是今天的、明天的和后天的，这是免费的心知天气所提供给我们的服务。如果我们注册用户的话能得到更多的信息，我们为了讲解就使用免费服务就够了。

那么在接下来讲解之后，我将使用我们刚才讲解的第一个实例程序，在这第一个实例程序的基础上做一下修改，这么做也是为了加强您对如何使用 ESP8266 利用 HTTP API 获取信息这个操作的理解。好，那么下面我们就把这第一个实例程序，也就是刚刚我给大家讲解完毕的第一个实例程序，我们在这第一个实例程序基础上做一下修改，我们把它修改成可以获取天气预报的这么一个程序。

好，我们来开始。首先我们要做的第一个修改是在这个程序的 `loop()` 函数中，这里这个请求资源地址的这个字符串我们要把它做一下修改。修改内容是什么呢？现在我们是字符串所做的这种内容是用来获取当前天气的这个请求资源地址，我们要把它改成什么呢？改成获取天气预报的请求资源地址。那这个天气预报请求资源地址在哪儿呢？这个就参考心知天气的这个说明文档了。

好，请看，现在这个页面中是天气实况的说明文档，我们要找的天气预报的说明文档在哪呢？请看左边我鼠标所指的地方，那这里有一个「逐日天气预报」和「昨日天气」这个链接，我们点击它。好，进入这个页面中，它就会告诉我们请求地址示例是什么了。这里北京今天和未来四天的预报信息，当然这是北京的，假如你要获取别的地方，只要改一下这个城市的信息就可以了。我们接下来就要利用这个地址示例，我们把它右键点击它，把它复制这个链接，我们把这个链接复制到一个文本编辑器中，因为我们要对这个链接进行一些修改内容。

请看，现在这个文本编辑器中，我把刚才复制链接粘贴到这里了。好，接下来我们要对这个链接搜一下修改。修改内容是什么呢？首先第一个地方我们要把前面这个主机，你看这个主机地址这些内容要给它去掉，因为我们只需要的是这个主机地址后面的后面的内容。为什么是这样的？因为主机地址并不需要在我们这个请求资源的这个字符串里面存放，那主机地址是在我们程序刚开始就已经设定好了，这里已经设定好这个主机的地址了，所以我们只需要斜杠 `v3` 后面的内容。好，我们现在把这个 `v3` 前面的内容把它删除掉。

然后我们要对剩下内容做进一步的修改。修改的地方是什么呢？首先第一个请看这个 `language=zh-Hans` 这个地方，这里是告诉服务器我们现在要获取的天气信息是中文的，但是我们要把它改成英文的。怎么改呢？经常把这个 `zh-Hans` 把它删掉，然后写上 `en`。好，通过这个修改，服务器知道我们要的是英文的信息了，这是第一个修改的地方。第二个修改的地方请看后面这里，从这里开始一直到这里是 `start=0`，你看它有点换行了。从这个 `start=0`、`days=5` 这部分内容呢，是告诉服务器我们要获取这个天气预报信息是从哪天开始到哪一天结束。

好，接下来我来把这个修改一下，修改好我再告诉你为什么这么修改。首先请看我把这个 `days=5` 这个地方，把这个 5 这个数字我们把它改成为什么是 3 的。请看，首先第一个这个数字 0 是代表我们从哪天开始，3 呢是我们一共需要几天的天气。好，0 就代表今天，也就是从今天开始一共给我三天的天气信息，这样服务器就会把今天的、明天的、后天的这三天的天气信息反馈给我们了。

那假设我们现在需要从明天开始到后天的天气信息怎么改呢？请看，我在这里修改一下，改成这里，`start` 是 1，1 呢是代表明天开始，那么只需要两天，明天、后天只有两天，把它改成 2，这个就是从明天开始到后天这两天天气信息。不过我还是把它恢复回来，因为我们现在需要的是从今天开始到后天这三天的天气信息。那这里我们这个修改信息就结束了。

接下来还有两点需要你留意一下。首先第一点就是这里一个 `key=` 后面的这个信息，这个信息我这里和您那里肯定是不一样的，因为这是我的私钥，肯定和您的私钥是不同的，这个地方跟您的是不一样的。第二个呢请看这个 `location`，`location` 现在我们是获取的是北京的天气信息，如果您想获取别的地方的天气信息，这里应该是别的城市的名称汉语拼音。那么到这里，我们这个整个这个字符串就把它修改完毕了。

接下来我们把它复制，并且我们把它粘贴到我们的程序中去。粘哪儿呢？你看到这个 `loop()` 函数中这个字符串中，好，我们把它把原来的这个旧有的信息删除掉，我们建立一个新的字符串。好，请看我把这个刚刚我们修改好的信息把它粘贴到这里了，现在这个是新的字符串信息了，这个就是获取天气预报这么一个信息的。好，其他我把这个注释改了，不再是获取当前天气了，而是获取天气预报的这个请求资源的地址了。

好了，做完这个修改以后，接下来我们来验证一下我们的开发板是不是已经可以成功地从心知天气服务器获取天气预报的信息了。怎么来验证呢？好，我们下面对我们程序做第二个修改。在这个函数中的你看，好，请看在这里我们将在这里进行一个修改，这部分内容是将要解析心知天气的响应信息，我们先别让它解析这个心知天气信息，尽量把这段程序把它做了一个注释，这样它就不会运行它了。

然后请注意，我将要建立一个新的字符串，字符串名字叫什么呢？就叫 `serverJson` 吧。好，这个新的字符串它将会存放内容是什么呢？它将会存放的就是你看我这敲的代码，好，请看这个新的字符串存放的内容就是服务器发过来的 JSON 的数据内容了。为什么呢？因为我们之前已经通过介绍跳过服务器响应信息中的响应头了，剩下的就是 JSON 的这个数据了，我们就 `readString` 把这个 JSON 数据转化成一个字符串格式的存储到这个 `serverJson` 这个词中，然后我们把这个字符串通过串口监视器把它说出来，看一看这个 JSON 数据是不是我们想要的心知天气服务器发给我们来的这个天气预报信息。

好了，现在程序成功地传上去开发板了，接下来我来打开串口监视器。我们看到打开串口监视器以后，开发板在不断地向我们输出信息，我们看看这个输出信息里面有没有服务器发给我们的这个 JSON 数据。好，请看到这里，那个底部这里，这个我鼠标高亮的部分内容呢，是一串很长很长的数据，这个数据就是服务器发给我们这个含有天气预报信息的这个 JSON 数据了。

好，接下来我们来把它复制，通过复制我们把它复制到哪里去呢？我们把它复制到一个能够调整这个 JSON 数据格式的这个地方，我们利用这个 JSON 在线格式化工具，我们把这个刚才复制的这份数据把它粘贴到这里。好，粘贴以后我们可以看到这个数据看起来格式清晰得多了。好，我们看这个里边都是什么东西。首先我们来看这里这个 `location` 所对应的这个信息内容，`location` 是英语位置的意思，自然了这个 `location` 所对应的内容就是反映出我们所申请的哪一个城市的位置，北京，我们所设定的是北京这个城市的天气信息，这个并不是我们最关注的。

我们最关注的是后面这个是 `daily`，这个后面这个 `daily` 所对应的这么一个数组，就是方括号，这个数组里面请看，从这里开始一直到到这里结束，整个这个数组里面一共有三个对象，是三天的天气预报的信息。请看这第一个对象里面，他告诉我这是 2020 年 6 月 12 号，这是我现在录课的这个日期，也就是 2020 年 6 月 12 号，是今天的天气情况。今天天气情况呢，白天晴 `sunny`，是晴，这个晴天的天气代码是 0，今天晚上的阴代码 4，今天最高气温 34 度，最低气温 23 度，降水概率 0%，以及这是风向南风等等等等，这些信息还挺丰富的，这是今天的天气信息。第二个对象呢，这是明天的 6 月 13 号，明天，6 月 14 号是后天的天气信息。这三个对象里面所存储的内容就是心知天气所发给我们的天气预报信息了。

好了，通过刚才这一系列操作，我们可以很肯定地知道，当前我们这个程序的修改结果是可以获取到心知天气的天气预报信息了。那获取到的天气预报信息目前是一个字符串格式，我们要把这个信息进行一个解析，那么具体怎么解析呢？我要继续这个程序进行修改了。好，我现在把刚才这个测试用的这个程序代码都把它删掉了，它们已经没有用了，这只是测试用的，我们在恢复刚才这个 `parseInfo` 这个函数的运行。

好，现在我们知道这个 client 里面存储的是服务器发来的这个天气预报的 JSON 信息了，我们要把这个 client 传递到 `parseInfo` 这个函数中去，并且请看这个 `parseInfo` 现在这个函数中它已经获取到了心知天气发来的天气信息了。接下来我们要对这个天气信息进行解析了，不过这个解析的过程和旧有的这个函数中的内容是完全不同的了，所以我要把旧函数中这个内容把它删掉，原来函数中作用全都给删掉了。

然后呢我们还回到刚才我们这个请看这个 JSON 在线格式化工具，这是我们获取到的 JSON 信息的这个整个的信息内容，我们把它全选，并且再把它复制到剪贴板中。然后我们来到 ArduinoJson 的官网，`arduinojson.org`，好，回车。进入 ArduinoJson 的官网以后，我们来进入到这个在线工具，点击一个 assistant 这个链接，进入这个在线工具的一个页面。到这页面以后，我们就要把刚才我们这个复制的 JSON 信息把它粘贴到这里了。当我把这个信息粘贴到这里以后，我们会看到这个页面自动给我生成好的这个解析代码了。好，我们现在就可以把这个生成好的解析代码把它复制，好，复制好以后，再把它粘贴到刚才这个 `parseInfo` 这个函数中去。

好了，现在这个代码给它粘贴好了以后，我们看一看是不是完整，哦，这里少了一个 `c`，要小心，复制粘贴过程中一定要把这个信息复制粘贴完整，否则这个程序会出错，所以一定要小心。好了，程序粘贴好以后，这个程序的格式对于我们来说非常非常重要，请大家在写程序的时候一定要注意。我把这个格式整个函数复制进来的这个内容的它的前面加一行空余，让这个格式更利于我们阅读。好了，这个调整好这个格式以后，接下来我们就要对这个程序做进一步的修改了。

为什么要做进一步修改呢？原因是我们现在粘贴这个程序，它所解析的内容是这里这个 JSON 这里这个信息内容，而我们实际上是让他来解析的是我们传递进来的这个 client 这个对象啊，所以我们先把这个 JSON 把它删掉。好，请看我把刚才的这个 JSON 它删掉了，删掉以后呢接下来请看我们把这个 client 传递进来的这个 client 把它粘贴到这里。在这里通过这个 `deserializeJson()` 这个函数，我们就会对这个 client 这个对象中所存储的 JSON 信息对它进行解析了。

假如你现在看我这个演示不太理解我们这个操作的整个过程是什么，那说明什么呢？说明您之前的课程可能有地方还不太理解，因为这些操作我在之前的课程都给大家讲过了。如果不太理解的话，那我建议您看看我们这课之前的内容，把这个怎么用 ArduinoJson 库解析 JSON 数据这个操作搞清楚，这样的话我们这个课程才能对您来说有意义，否则的话接下来你越听会越糊涂。

那么做完这个修改以后，我们对这个函数的整个的修改那就基本完成了。下面我们就可以利用我们这个函数中所解析出来这个内容，把这个内容输出到串口监视器，我们来看一看我们是不是的的确确已经获取到了这个天气信息了。我们来找一个比较有代表性的一个数值，好，就这个吧，我们就会来获取这里这个 `text`，这个 `text` 这里所存储的内容是后天的天气预报中这个白天的天气情况。好，我们就把这里这个数值把它提出来，提取出来，然后请看，我在这里把这段高亮的内容把它复制，然后我们到一个函数中一个空的地方，啊，就这里吧，我们把它粘贴到这里。

好，然后我们要对这个内容进行一些加工，才能把这里所存储的数值把它提取出来。怎么加工呢？首先我建了一个变量，我们知道这里面所存储的是一个字符串型的一个内容，于是我们建了一个字符串的变量，就叫 `day2Text`，等于什么呢？等于这里面所存储的内容。但是它所存的内容是一个常量指针，我们要对它进行一个转换，使用 `as` 函数把它转换成字符串类型，我们就把当前这里是存储的后天的白天的天气情况用这个字符串提取出来了。然后我来通过串口监视器把这个字符串的具体信息把它输出出来，以便我们观察。好了，到这里我们就可以把这个信息把它输出出来了，一会我们把它编译并且看一看我们这个程序是不是确确实实可以获取到后天白天的天气情况了。

好了，现在程序成功地传上去开发板了，接下来我来打开串口监视器，好，我们也看到开发板在不断输出信息，为了便于观察把这个自动滚屏关闭。好，请看，在这个程序告诉我它已经找到响应头的结尾了以后，紧跟着它就输出了一个 `sunny`，`sunny` 是什么呢？正是刚刚我们的程序里面通过这里这个 `println` 这里输出的这个内容，这个内容正是我们后天通过这里所获得到的后天白天的天气情况。

为了验证，我们再来做一个实验，这次我们来获取一个明天白天的最高气温吧。好，在这里这个这个 JSON 对象所对应的明天的最高气温，好多信息哈，那这个信息其实挺多的，有时候看起来有点乱。好，在这里我们会看到，这是明天白天最高气温，我们把这个地方把它复制，然后我们还同样地来到整个函数的结尾的地方，好，把刚才这个复制的内容把它粘贴到这里，并且对它进行一个转换。

我们知道明天的这个白天的最高气温是一个整数型的，我们把它转换成 int 类型的，另外我们再建立一个变量来存储这个信息，好，这叫 `day1High`，等于，好，现在这个整数型的变量里面所存储的内容就是明天白天最高气温。我呢同样的还是通过串口监视器把这个信息把它输出出来。好了，现在明天白天最高气温这个信息已经可以通过串口输出了，我把这个程序再次地编译并上传开发板，我们看看这个运行的情况。

好了，现在程序成功地传上去开发板了，接下来我来打开串口监视器。好，我们打开串口监视器可以看到，我们这个 `sunny` 就是刚才的后天的白天天气情况是 `sunny`，也就是晴天。在这个下面请看有一个 33，这是明天白天最高气温 33 度，没有问题。通过我们刚才这一系列操作，我们会很肯定地知道，我们已经把心知天气的天气预报信息解析，并且可以通过一系列操作把这些信息获取到应用到我们的程序中去了。

好了，通过刚才的讲解，我们就把如何使用实例一这个程序来获取天气预报信息的这个改编讲解完毕了。通过以上的讲解，如何使用心知天气的这个平台的使用方法，而更重要的，是想让你掌握一个新的技能，这个技能是什么呢？就是如何来使用 ESP8266 来获取 HTTP API 的信息。通常这种 HTTP API 它们所发布的信息都是 JSON 格式的。那我们在使用一个新的 HTTP API 平台的时候，首先第一步是要先找到这个平台的使用介绍说明。

通常在这个平台的这个产品文档中，它会告诉我们具体发送什么样的请求地址会得到什么样的信息，以及在这个请求地址中哪些主要参数、这些参数如何来进行设置。这些说明是非常关键的，掌握了说明以后，我们就可以自己来建立我们所需要的这个请求的地址的内容了，并且把这个内容写到我们这个程序中去。然后当我们可以获取到这个平台所发布给我们的信息以后，接下来我们就可以把这个信息利用 ArduinoJson 库来把它解析出来。这个是我们想要给你讲解的主要内容。

那在这里我想再强调一下，就是希望您能够把我们这节课所讲解的知识务必掌握，你如何使用 HTTP API 这个技能，对于使用物联网还是非常关键的，我相信您在未来物联网开发的过程中肯定会接触到这样的应用和操作。接下来我们还回到刚才我们讲解了这个心知天气中来。这个心知天气，刚才如果您是跟着我们把这个实例程序把它改编成能够获取天气预报这么一个改变过程的话，如果你在跟着我们操作的时候有些吃力，或者哪一点不太明白的话，没关系，在我们这个实例一下面就是有我们改编好的这个实例程序。

请看这个实例二，如何使用心知天气获取天气预报信息，这个实例程序在这里就有了。那这个程序您可以看一看，如果您刚才跟着我们操作的时候有困难的话，看这个问题在哪里，这是我们第二个实例程序。实际上在我们的页面中还有一个实例程序，请看这个实例三，这个实例程序所使用的知识内容其实在我们之前的课程都已经讲过了，我在这里就不再重复地给大家讲解了。我建议你把这个当作一个作业，那么如何使用心知天气来获取生活指数，我建议您自己来实现这个功能。

具体这个心知天气网站中如何来使用生活指数这个服务的说明文档，同样在这个文档的这个产品文档中，在心知天气数据接口里面有一个天气类接口文档，在这个文档中请看，这是我们刚才给大家讲过的天气实况这个文档，在这个文档之后请看，在左侧这里有一个生活类，然后底下这个生活指数。好，在这个文档中就是具体如何来通过心知天气获取生活指数的这个请求地址实例了，以及这个请求地址实例下面请看这里是实例中需要用到各个参数的说明，这些内容都跟我们之前程序给大家讲解的内容都是一样的。

到目前为止，使用心知天气服务的准备工作，以及如何来编写程序获取并解析天气信息，不过这个程序说实话挺长的，你看，这是我们刚才写的如何来获取心知天气的这个天气预报的这个程序，一共有 180 多行。是想每次我们要想开发一个新的项目的时候，要想获得心知天气的天气信息，都要写这么长这个程序，是一个挺麻烦的事情。有没有什么更简单的方法呢？当然有了，我们呢已经开发了一个专门用于心知天气的一个库，心知天气的这个使用流程只需要简单的几行代码就可以得到你想要的信息内容了。

那么接下来我来给你介绍一下如何来使用这个库。这个库是专门针对心知天气的免费服务所开发的，所谓免费服务的就是实时天气信息、天气预报信息，还有实时生活指数信息，具体内容在这括号里面可以看到。那么要想使用这个库，我们需要先做一些准备工作，第一个就是先注册心知天气账户了，这个我们已经操作好了。第二个呢就是先把这个库下载到你的电脑中去，并且把它安装到 Arduino IDE 中去。

如何下载呢？请看这里有一系列的链接，可以通过网盘、GitHub 或者码云都可以下载，下载好以后再安装到 Arduino IDE 中。这个操作如果你不会，你可以看到我们这右边这个常见问题解答，这里有一个链接，这里有个图文教程教你怎么做。做完以后呢，接下来还有一个非常重要的工作，就是要把这个 ArduinoJson 库也要装到你的 Arduino IDE 中去，用这个心知天气库也需要用到 ArduinoJson 库，但这些操作在我们之前讲解中都已经给你讲解完毕了。

下面我们就可以开始使用这个库了。第一个实例程序请看这里，这个实例程序是来获取实时天气信息的实例程序，我们把这个实例程序把它复制并粘贴到 Arduino IDE 中。好了，现在程序已经粘贴到 Arduino IDE 中了，接下来我们看这段实例程序的主要内容。首先一上来两个 `#include`，第一个呢使用 `ESP8266WiFi` 库，第二个呢是要使用这个呢是什么？这是我们的 ESP8266 心知天气的这个库了，你们必须要先把这个库安装好然后才能用，就没什么好说的。

把您的这个联网的名称和密码填到这里，紧跟在这里这三个字符串的第一个，这是 `reqUserKey`，这是写您的心知天气私钥的地方，那下面我就把我的心知天气的私钥把它放到这里，好，这里就是我的信息，就是私钥信息了。然后请看，接下来这个地方就是输入我们要请求哪一个城市的天气，就是北京的，所有的这些是北京的，然后底下的就是我们需要获取的是摄氏度，这些都和之前是一样的，不浪费时间了。接下来这里这是一个非常重要的一句，我们建了一个 `weatherNow` 的对象，对象名称也叫 `weatherNow`。

`weatherNow` 什么东西啊？`weather` 请看前面这个是天气的意思，`now` 呢现在的意思，那通过这个对象我们就可以利用心知天气来获取当前的天气信息。注意，这个对象只能获取当前天气信息，如果我们想要获取天气预报信息怎么办呢？我们不能用这一句了，我们要换一个语句，我们要用 `forecast`，就预报的意思，因为 `forecast` 建立一个对象，就随便起一个名字就叫 `forecast`，那么这个对象就可以获取天气预报的信息了。不过我们这个程序主要是用来获取当前天气信息的，所以我在这里先建立这么一个对象就可以了，后边再给你讲其他的使用方法。

好，这个对象建立好以后，紧跟着就进入到 `setup()` 函数了。`setup()` 函数请看启动串口通讯，然后连接 WiFi，就连接 WiFi 这个函数所实现的方法呢我们之前已经讲过很多次了，不浪费时间了。我们继续往下看。好，紧跟着请看这里这条语句，这条语句非常关键，这条语句我们将对我们的心知天气所请求的信息进行一个设置，请看这条语句呢，调用的是 `config` 这么一个函数，就是 ESP8266 心知天气这个库中的一个叫做 `config` 的这么一个函数，这个函数将会把我们的私钥信息以及我们请求哪一个城市的信息以及使用什么单位，把这些信息收集好，然后我们就可以在后面的程序中对心知天气发送请求获取天气信息了。这个是 `config` 函数的作用。

我们继续往下看，接下来我们来到程序的 `loop()` 函数中，`loop()` 函数中一看有一系列的语句，看起来挺复杂的，其实特别简单。我们在这里首先请看使用一个逻辑判断语句，这个逻辑判断语句中的判断条件是什么呢？是利用 `weatherNow` 这个对象来调用 `update` 函数。通过 `update` 函数，我们的开发板将会向心知天气服务器发送请求，请求什么呢？请求当前的天气信息。那么服务器在收到我们这个开发板的请求以后，将会把当前的天气信息反馈给我们的开发板。

但是我们这里这个 `update` 函数它还有一个返回值，这个返回值是一个布尔型的。当这个开发板成功地连接到服务器了，并且通过服务器收到了这个响应代码是 200 的时候，这个时候我们这个 `update` 就会返回真。注意，我这两个条件，第一个是成功连接到服务器，第二个是服务器的响应代码是 200，这个时候说明服务器已经找到了我们想要请求的信息了，所以这个时候 `update` 就会返回真。如果 `update` 返回真的时候，请看程序将会执行这个 `if` 条件判断中的请看这里这些语句，这一系列语句内容就是将我们这个开发板所获取到的这个天气信息通过串口监视器输出出来，这是成功地连接上并且响应状态码是 200 执行这里。

否则的话，如果连接失败或者是响应状态码不是 200，那么就通过串口监视器告诉我们信息更新失败了，以及通过串口告诉我们具体这个服务器响应状态码是多少，通过这里输出出来，让我们来判断一下这个信息更新失败的原因是什么。这个呢是没有成功获取到服务器的天气信息所执行的内容。那我们来看看成功获取到天气信息的时候所执行的内容。好，请看这里，这里是一系列的串口输出，这些串口输出就是把获取到的服务器信息说出来。

首先请看第一句告诉我们以下是收到的服务器信息，首先告诉我们这个服务器响应状态码是多少，我们这个 server response，然后请看通过这条语句，这条语句是什么呢？调用心知天气这个库中的这个 `getServerCode`，就是获取服务器代码，把这个服务器状态码输出出来，让我们看一看是什么内容。然后将会通过这里请看这条语句，这条语句调用的是这个 `getWeather`，获取天气文字，什么文字呢？就是当前的天气信息的文字，那是一个字符串型的文字内容，它会把当前的天气信息通过字符串进行返回，然后我们再通过串口监视器把它输出出来，就是字符串格式的。下面这个请看这里是把天气信息代码把它输出出来，然后就是当前的温度信息。

这些函数，你看我鼠标高亮这些函数，都是我们的 ESP8266 心知天气库中的这个库函数。那么您也可以通过这些程序内容来获取到心知天气的信息，只要通过这么几个函数就可以把这些信息获取到了，而无需像我们之前的程序中还要通过什么 ArduinoJson 库对这个 JSON 数据进行解析，然后再对一个个的数据再进行提取，这些都不需要，只要简单地请看，简单地调用几个函数，您的这个信息就可以得到了。好，另外最后请看这里还有个库函数，这库函数就是获取当前服务器更新这个天气信息的时间是什么。通过这一系列的讲解，我们这个 `weatherNow` 请看这个 `weatherNow` 这个对象就给你讲解完毕了，这个就是获取当前天气信息的这么一个实例程序。

那下面我们来简单看一看如果想要获取天气预报信息的实例程序。想要获取天气预报这个实例程序，实际上你可以通过 Arduino IDE 直接打开的。当你把这个 ESP8266 心知天气库装到 IDE 以后，通过「文件」，然后这里这个「示例」，找到我们这个心知天气这个库的名字，请看 ESP8266 心知天气这个库的名字，然后在这 example 里面，请看这里有一系列的实例程序。好，其中这个刚才给大家讲解的是 `weatherNow`，也就是获取实时天气的这个实例程序，在这里。我们先来看看这个天气预报这个实例程序，也就是 `forecast` 这个实例程序，我们点击它。好，打开的这个新程序就是获取天气预报的实例程序了。

在这个实例程序中，基本上它的实现方法和我刚才给大家讲解的过程都是很相似的。首先连接网络，然后输入一系列的这个心知天气的这个信息内容，然后请看在这个 `setup()` 前面这里非常重要，也刚才也讲过，如果想获取天气预报，我们就要建立一个叫做 `forecast` 的一个对象，对象名称呢叫做 `forecast`，`forecastUnit`，我们首先还是一定要请看使用这个 `config` 这个函数来对这个心知天气的响应信息进行配置，一个私钥，获取哪个城市的信息，以及单位是什么。这个 `config` 函数同样在 `setup()` 之中进行调用。然后请看在这个 `loop()` 函数中，我们同样用这个 `update` 这个函数，让开发板向服务器发送请求，如果请求成功的话，接下来就可以把这个信息获取到了。

那通过什么呢？还是通过一系列的库函数，你看，`forecast` 利用这个什么 `getDataText`，就是获取白天的天气情况的这么一个库函数。他这个库函数请看它有一个参数，这个参数 `i` 是什么呢？我们知道我们想获取的一个天气信息是由今天的、明天的、后天的三天的天气信息，这个 `i` 就是代表获取哪一天天气信息。当 `i` 是 0 的时候，获取的就是今天的天气，`i` 是 1 的时候呢是明天，`i` 是 2 的时候就是后天。那么这个 `getDataText` 获取天气的信息情况，具体这个 `forecast` 这个对象有多少库函数呢？我们请看在这个网页中有说明。

请看这里，是刚刚给大家讲的这个实时天气信息实例程序，在这个实例程序的下方请看 2.2 这里获取天气预报信息实例程序，在这个章节里面请看我们给您列出了所有能够用 `forecast` 这个对象所调用的这个库函数，以及简短的说明，比如说用什么函数获取白天天气信息、还是晚上的、还是最高气温、最低气温等等等等。那么通过这一系列的这个库函数，你可以轻而易举地把你想要的信息得到了，同样的也省去了很多麻烦，为什么通过 ArduinoJson 库解析数据这些都不需要，只要通过几个函数就可以实现了。这个呢是 `forecast` 也就是获取天气预报的这个操作方法。

另外在这个页面中还有第三个实例程序，这第三个实例程序是如何来使用心知天气获取生活指数信息的，这些实例程序比如说或者穿衣建议、运动建议、紫外线这些建议信息都可以获取到。那么这个实例程序就在这里，它所使用的知识内容和我们刚才这几个实例程序都非常相似，我就不在这儿重复了。好，到这里我们就给您讲解完毕了关于如何使用这个 ESP8266 心知天气库。

好了，到这里我们这节课我要给你介绍的主要内容基本就要结束了。不过最后还有一个非常非常重要的知识我讲给您，请您务必要留心。当我们在使用不同的 HTTP API 平台的时候，我们要小心不同的平台给我们这个使用的限制和要求是什么。这个限制要求，比如说我们这个心知天气这个平台来说，请看，当我们进入这个心知天气这个控制台以后，请看它这个「添加产品」，点击这个链接，这里列出了所有这些产品的一些要求。我们现在使用的是免费版的这个产品，你看这个免费版产品它对这个访问频率有个要求。

什么是访问频率呢？这个访问频率的意思就是我们在使用开发板或者用电脑向心知天气服务发送请求的时候，这个访问的频率不能太快。请看它这里所写的，我们这个访问的频率上限是 20 次每分钟，那么平均下来的基本就是三秒钟一次，不能超过这个频率。如果超过这个的话，那可能轻则我们这个 IP 封了，重则没准我们这个账户就都用不了了。第二个要注意的地方，请看下这个访问限量，对我们这个免费版的产品来说它是没有访问量的限制的，那对于试用版来说请看它是有一个访问量的限制，还是 1 万次的访问量。可能有别的平台它也会有各种各样不同的一个方面的限制，这块我们要注意，不要把这个访问量用得过大，用完量以后我们就没法用这个平台了。

好了，以上就是如何使用 ESP8266 来通过 HTTP API 获取信息的介绍讲解了。还是那句话，所以我们这节课主要讲解的是使用心知天气这个平台，事实上我们是想用这个平台作为一个示例向您讲解如何来通过 HTTP API 来获取信息这个操作。好，那么这节课内容到这里就结束了，非常非常感谢您，我们下节课再见。

---

## 本节要点

- 本节在实例一的基础上做改造，把「获取实时天气」改成「获取天气预报」，加深对 HTTP API 调用流程的理解。
- 天气预报的请求地址要从产品文档的「逐日天气预报」页面获取，复制时要去掉主机地址部分，只保留斜杠 `v3` 之后的内容。
- 请求地址中的 `days` 参数控制预报天数（免费服务最多三天），`start` 参数控制从哪天开始，0 代表今天、1 代表明天。
- 地址中的 `language` 参数默认是 `zh-Hans`（中文），改成 `en` 就是让服务器返回英文响应；`key` 参数填自己的私钥，`location` 填城市的汉语拼音。
- 验证阶段先把 `parseInfo` 的解析代码注释掉，另建 `serverJson` 字符串用 `readString` 接住响应体，再用 JSON 在线格式化工具检查结构，确认 B 已拿到天气预报数据。
- 返回的 JSON 中 `location` 表明城市，真正的数据在 `daily` 数组里，数组含三个对象，分别对应今天、明天、后天，每个对象包含白天的天气文字、天气代码、最高最低气温、降水概率和风向等。
- 确认结构后，把 JSON 粘贴到 `arduinojson.org` 的 assistant 在线工具生成解析代码，替换掉旧函数内容，并把要解析的对象换成传递进来的 client 对象，用 `deserializeJson()` 完成解析。
- 取用数据时要注意类型转换：字符串型的要用 `as` 函数转成字符串，整数型的（如明天白天最高气温）要转换成 int 型再存入变量。
- 通用方法论：使用新的 HTTP API 平台，第一步是找到平台的产品文档，看清请求地址示例与各参数的设置方法，然后照着写请求地址，再用 ArduinoJson 库解析返回的 JSON。
- 心知天气官方提供了专门的库，只需几行代码即可获取数据，省去手写长请求地址和 JSON 解析的麻烦；使用前需先注册账户、安装该库并同时安装 ArduinoJson 库。
- 心知天气库用法要点：用 `config()` 配置私钥、城市和单位，用 `update()` 发送请求（连接成功且状态码为 200 时返回真），再用 `getServerCode()`、`getWeather()`、`getTemperature()` 等库函数取值。
- `weatherNow` 对象用于获取实时天气，`forecast` 对象用于获取天气预报；`forecast` 的取值函数带参数 `i`，0/1/2 分别代表今天、明天、后天，可配合 `getDataText()` 等函数取白天或夜间天气、最高最低气温。
- 使用 API 平台要留意限制：心知天气免费版访问频率上限为每分钟 20 次（约每三秒一次），超过可能被封 IP 甚至停用账户；免费版无访问量限制，但试用版有 1 万次访问量的限制。

---

*本文稿由 AI 从视频字幕整理，如技术描述与原视频有出入，请以原视频为准。*
